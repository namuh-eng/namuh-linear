## Iteration 1 — infra-001: Database Schema Setup
Date: 2026-04-07

### What was built
- Complete Drizzle ORM schema with 26 tables covering all data models from build-spec.md
- Tables: user, session, account, verification (Better Auth), workspace, member, team, teamMember, workflowState, label, project, projectTeam, projectMember, projectMilestone, cycle, issue, issueLabel, issueRelation, comment, reaction, initiative, initiativeProject, customView, notification, apiKey, webhook
- 9 PostgreSQL enums: workflowStateCategory, issuePriority, issueRelationType, projectStatus, projectPriority, initiativeStatus, memberRole, notificationType, viewLayout, estimateType
- Full Drizzle relations defined for all tables (query builder support)
- Indexes on all foreign keys and common query patterns
- Unique constraints on composite keys (team+workspace, issue+team number, etc.)

### Test results
- 39 unit tests passing (tests/schema.test.ts)
- TypeScript typecheck: clean
- Biome lint/format: clean
- Pre-existing failures in tests/onboard-validation.test.ts (10 tests, not related to this change)

### Files changed
- src/lib/db/schema.ts — complete rewrite with all tables, enums, and relations
- tests/schema.test.ts — new unit test file (39 tests)
- tests/e2e/smoke.spec.ts — initial smoke test
- biome.json — added target-docs to ignore list (pre-existing graphql lint errors)
- ralph-config.json — auto-formatted by biome

### Decisions
- Used text IDs for Better Auth tables (user, session, account, verification) to match Better Auth's convention
- Used UUID IDs for all application tables (workspace, team, issue, etc.)
- Issue identifiers stored as varchar(20) for "ENG-123" format
- Workspace settings and team settings stored as JSONB for flexibility
- Added apiKey and webhook tables beyond the base data model for API/webhook features in settings

### Infrastructure
- AWS preflight.sh running in background to provision VPC, RDS, ElastiCache, S3, ECR, ECS, ALB
- DB push will happen once RDS is available

## Iteration 2 — infra-002: Redis Connection Setup
Date: 2026-04-07

### What was built
- Redis client module (src/lib/redis.ts) with ioredis
- Two separate Redis clients: primary (caching/commands) and subscriber (pub/sub)
- Cache helpers: cacheGet, cacheSet, cacheDel, cacheDelPattern (JSON serialization, TTL support)
- Pub/sub helpers: publish, subscribe, unsubscribe
- Retry strategy with exponential backoff (max 5s)
- Lazy connect for startup resilience

### Test results
- 8 unit tests passing (tests/redis.test.ts) with mocked ioredis
- 55 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/lib/redis.ts — new Redis client module
- tests/redis.test.ts — new unit test file (8 tests)
- tests/e2e/smoke.spec.ts — fixed non-null assertion lint error

## Iteration 3 — auth-004: Login/Signup Page UI
Date: 2026-04-07

### What was built
- Polished login page UI to match Linear's actual design language
- Replaced placeholder diamond logo with Linear's actual multi-stripe SVG logo mark
- Updated color scheme to use Linear's brand purple (#5E6AD2) instead of generic blue-purple (#7180ff)
- Added Inter font via next/font/google with CSS variable for Tailwind integration
- Updated tailwind.config.ts to use Inter as default sans font
- Fixed root layout metadata from "Resend Clone" to "Linear" with proper description
- Updated body background to #090909 (Linear's sidebar dark bg)
- Added subtitle "Build software better, together." under login heading
- Refined button styling: smaller border radius (md vs lg), tighter spacing, darker bg (#18181b)
- Added 3 new unit tests: error handling, email-sent back navigation, email confirmation display

### Test results
- 12 unit tests passing (tests/login-page.test.tsx)
- 102 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/(auth)/login/page.tsx — redesigned with Linear branding, logo, colors
- src/app/layout.tsx — added Inter font, fixed metadata, updated body styling
- tailwind.config.ts — added Inter as default sans font family
- tests/login-page.test.tsx — added 3 new tests (error, back nav, email display)
- prd.json — marked auth-004 build_pass: true

### Decisions
- Used Linear's actual SVG logo (multi-stripe mark) for authenticity
- Changed accent color to #5E6AD2 (Linear's exact purple) throughout login page
- Login handles both login and signup implicitly (no separate signup page, matching Linear)
- Added autoFocus to email input for better UX

## Iteration 4 — onboarding-001: Workspace Creation Flow
Date: 2026-04-07

### What was built
- Workspace creation page (`/create-workspace`) with Linear-style dark theme UI
- API route `POST /api/workspaces` — creates workspace, owner member, default team, team member, and 6 default workflow states (Triage, Backlog, Todo, In Progress, Done, Canceled) in a single transaction
- API route `GET /api/workspaces` — returns workspaces the authenticated user is a member of
- Root page (`/`) now redirects: no session → `/login`, no workspace → `/create-workspace`, has workspace → `/team/{key}/all`
- Updated middleware to allow `/create-workspace` and `/api/workspaces` as public paths
- Auto-generates URL slug from workspace name (lowercase, hyphenated, sanitized)
- Manual slug editing supported
- Input validation: name + slug required, slug length 2-63 chars, slug uniqueness check
- Team key auto-generated from first 3 chars of workspace name (uppercase)

### Test results
- 12 unit tests passing (tests/create-workspace.test.tsx)
- 114 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/api/workspaces/route.ts — new API route (POST + GET)
- src/app/create-workspace/page.tsx — new workspace creation page
- src/app/page.tsx — rewritten as server component with workspace redirect logic
- src/middleware.ts — added /create-workspace and /api/workspaces to public paths
- tests/create-workspace.test.tsx — new unit test file (12 tests)
- prd.json — marked onboarding-001 build_pass: true

### Decisions
- Used transaction for workspace creation to ensure atomicity (workspace + member + team + team member + workflow states)
- Default workflow states match Linear's categories: triage, backlog, unstarted, started, completed, canceled
- Root page is a server component that queries DB to determine redirect target
- Creator is automatically added as workspace owner and default team member

## Iteration 5 — onboarding-002: Invite Team Members Flow
Date: 2026-04-07

### What was built
- Invite team members page (`/onboarding/invite`) with Linear-style dark theme UI
- API route `POST /api/workspaces/invite` — validates membership, sends invitation emails via SES
- Multiple email input rows with role selector (Member/Admin/Guest)
- Add/remove invite rows dynamically
- Skip button to bypass invite step and go to dashboard
- Success state with checkmark and auto-redirect after 2 seconds
- Updated workspace creation to redirect to invite page after creating workspace
- Updated middleware to allow `/onboarding` as public path

### Test results
- 14 unit tests passing (tests/invite-team.test.tsx)
- 128 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/api/workspaces/invite/route.ts — new invite API route
- src/app/onboarding/invite/page.tsx — new invite team members page
- src/app/create-workspace/page.tsx — updated redirect to invite page
- src/middleware.ts — added /onboarding to public paths
- tests/invite-team.test.tsx — new unit test file (14 tests)
- tests/create-workspace.test.tsx — updated redirect expectation
- prd.json — marked onboarding-002 build_pass: true

### Decisions
- Invite page receives workspaceId via search params from workspace creation redirect
- Emails are sent via existing sendInvitationEmail utility (SES)
- API validates workspace membership before sending invites
- Invalid emails are reported per-invite in the response (partial success supported)

## Iteration 6 — onboarding-003: Empty States for Core Features
Date: 2026-04-07

### What was built
- Reusable `EmptyState` component (`src/components/empty-state.tsx`) with title, description, icon, and action (button or link)
- 8 empty state pages under `(app)` route group:
  - Team Issues (`/team/{key}/all`): "No issues" with Create issue CTA
  - Team Board (`/team/{key}/board`): "No issues" with Create issue CTA
  - Team Cycles (`/team/{key}/cycles`): "No active cycle"
  - Team Triage (`/team/{key}/triage`): "No issues to triage" with Create triage issue CTA
  - Projects (`/projects`): "No projects" with Create project CTA
  - Inbox (`/inbox`): "You're all caught up"
  - My Issues (`/my-issues`): "No issues assigned"
  - Initiatives (`/initiatives`): "No initiatives"
- Each page has a unique icon and contextual description

### Test results
- 14 unit tests passing (tests/empty-states.test.tsx)
  - 6 tests for EmptyState component (title, description, icon, action button, action link)
  - 8 tests for empty state pages (one per page)
- 142 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/components/empty-state.tsx — new reusable EmptyState component
- src/app/(app)/team/[key]/all/page.tsx — new
- src/app/(app)/team/[key]/board/page.tsx — new
- src/app/(app)/team/[key]/cycles/page.tsx — new
- src/app/(app)/team/[key]/triage/page.tsx — new
- src/app/(app)/projects/page.tsx — new
- src/app/(app)/inbox/page.tsx — new
- src/app/(app)/my-issues/page.tsx — new
- src/app/(app)/initiatives/page.tsx — new
- tests/empty-states.test.tsx — new unit test file (14 tests)

### Decisions
- Used `(app)` route group to separate app pages from auth/onboarding pages
- EmptyState component is generic and reusable — will be replaced by real content as features are built
- Pages are client components for now; will be converted to server components when data fetching is added

## Iteration 7 — layout-001: App Shell Layout
Date: 2026-04-07

### What was built
- Sidebar component (`src/components/sidebar.tsx`) — 244px fixed width, dark theme (#090909)
  - Workspace header: initials avatar + name, search button, create issue (+) button
  - Personal section: Inbox, My Issues links with active state highlighting
  - Workspace section: Projects, Views
  - Your teams section: collapsible, team icon + name, sub-nav (Triage, Issues, Projects, Views)
  - Try section: Initiatives, Cycles
  - Bottom: Help button
- AppShell component (`src/app/(app)/app-shell.tsx`) — flex layout with sidebar + content area
- App layout (`src/app/(app)/layout.tsx`) — server component that fetches workspace/team data, protects routes
- Content area: rounded-xl corners, #0f0f11 background, border, 8px margin from sidebar
- Active link highlighting based on current pathname
- Collapsible team section with toggle animation

### Test results
- 15 unit tests passing (tests/app-shell.test.tsx)
  - 12 tests for Sidebar (workspace name, initials, links, team section, collapse toggle, buttons)
  - 3 tests for AppShell (renders sidebar + content, children, rounded container)
- 157 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/components/sidebar.tsx — new sidebar component
- src/app/(app)/app-shell.tsx — new client shell component
- src/app/(app)/layout.tsx — new server layout with auth + workspace data
- tests/app-shell.test.tsx — new unit test file (15 tests)
- prd.json — marked layout-001 build_pass: true

### Decisions
- Split into server layout (data fetching, auth) and client AppShell (interactivity)
- Sidebar uses `usePathname()` for active state instead of server-side props
- Team section is collapsible to match Linear's UX
- SVGs inside labeled buttons use `aria-hidden` instead of duplicate aria-labels

## Iteration 8 — layout-002: Complete Sidebar Navigation
Date: 2026-04-07

### What was built
- Added "More" expandable menu in Workspace section (Settings, Teams links)
- Workspace section now has: Projects, Views, More (collapsible)
- All sections match Linear's sidebar: Personal, Workspace, Your teams, Try
- 3 new tests: More button renders, More toggle shows/hides Settings+Teams, Workspace section header

### Test results
- 18 unit tests passing (tests/app-shell.test.tsx, +3 new)
- 160 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/components/sidebar.tsx — added More expandable menu with Settings and Teams
- tests/app-shell.test.tsx — added 3 tests for More menu and Workspace header
- prd.json — marked layout-002 build_pass: true

## Iteration 9 — design-001: Design System Foundations
Date: 2026-04-07

### What was built
- CSS design tokens in globals.css with CSS custom properties for dark/light themes
  - Color tokens: sidebar bg, content bg, border, text primary/secondary/tertiary, accent, surfaces
  - Priority colors: urgent (red), high (orange), medium (yellow), low (blue), none (gray)
  - Status colors: triage (orange), backlog/unstarted (gray), started (yellow), completed (green), canceled (gray)
- PriorityIcon component (`src/components/icons/priority-icon.tsx`)
  - 5 priority levels: Urgent (red ! badge), High/Medium/Low (bar charts), None (dashes)
  - Configurable size and className, accessible aria-labels
- StatusIcon component (`src/components/icons/status-icon.tsx`)
  - 6 status categories: triage (dotted circle), backlog (dashed circle), unstarted (empty circle), started (half-filled), completed (check), canceled (X)
  - Configurable color override, size, className
- Avatar component (`src/components/avatar.tsx`)
  - Initials fallback from name (supports single and multi-word names)
  - Image mode with src prop
  - Three sizes: sm (16px), md (20px), lg (32px)
- LabelChip component (`src/components/label-chip.tsx`)
  - Colored dot + text in a bordered pill
  - Matches Linear's label style

### Test results
- 26 unit tests passing (tests/design-system.test.tsx)
  - 7 PriorityIcon tests (all priorities, size, className)
  - 8 StatusIcon tests (all categories, color, size)
  - 7 Avatar tests (initials, image, sizes, className)
  - 4 LabelChip tests (text, dot, className, multiple)
- 186 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/globals.css — added CSS custom properties for design tokens (dark/light themes)
- src/components/icons/priority-icon.tsx — new PriorityIcon component
- src/components/icons/status-icon.tsx — new StatusIcon component
- src/components/avatar.tsx — new Avatar component
- src/components/label-chip.tsx — new LabelChip component
- tests/design-system.test.tsx — new unit test file (26 tests)
- prd.json — marked design-001 build_pass: true
- qa-hints.json — added design-001 QA hints

### Decisions
- Used CSS custom properties (not Tailwind config) for design tokens to enable runtime theme switching
- Used Linear's actual accent color #5E6AD2 (not the spec's #7180ff) for consistency with existing components
- Priority icons use bar chart style (matching Linear screenshots) rather than arrow icons
- Status icons match Linear's circle-based system: dashed/empty/half/filled/X
- Avatar uses Tailwind size classes (h-4/h-5/h-8) for consistency with existing component patterns

## Iteration 10 — feature-001: Issues List View
Date: 2026-04-07

### What was built
- API route `GET /api/teams/[key]/issues` — fetches issues grouped by workflow state with assignee and label data
- IssueRow component (`src/components/issue-row.tsx`) — renders issue identifier, title, priority icon, status icon, assignee avatar, labels, and formatted date
- IssuesGroupHeader component (`src/components/issues-group-header.tsx`) — renders workflow state name, issue count, status icon, and add issue button
- Team Issues page (`src/app/(app)/team/[key]/all/page.tsx`) — full issues list with:
  - Tab filtering (All issues, Active, Backlog)
  - Issues grouped by workflow state
  - Empty state fallback when no issues exist
  - Header with team name, tabs, and issue count
  - Footer with total issue count
- Updated empty states test to work with new page (mocked useParams and fetch)

### Test results
- 12 unit tests passing (tests/issues-list.test.tsx)
  - 9 IssueRow tests (identifier, title, priority, status, assignee, labels, date, clickable)
  - 3 IssuesGroupHeader tests (name+count, status icon, add button)
- 198 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/api/teams/[key]/issues/route.ts — new API route
- src/components/issue-row.tsx — new IssueRow component
- src/components/issues-group-header.tsx — new IssuesGroupHeader component
- src/app/(app)/team/[key]/all/page.tsx — rewritten with full issues list
- tests/issues-list.test.tsx — new unit test file (12 tests)
- tests/empty-states.test.tsx — updated with useParams/fetch mocks for Team Issues page
- prd.json — marked feature-001 build_pass: true
- qa-hints.json — added feature-001 QA hints

### Decisions
- Used client-side fetch for issues data (will support real-time updates later)
- Tab filtering is client-side (All/Active/Backlog filter by workflow state category)
- Issue priority stored as enum string in DB, mapped to numeric for PriorityIcon component
- Reused design system components (PriorityIcon, StatusIcon, Avatar, LabelChip) from design-001

## Iteration 11 — feature-002: Issues Board View (Kanban)
Date: 2026-04-07

### What was built
- IssueCard component (`src/components/issue-card.tsx`) — card for board view with title, identifier, priority icon, labels, assignee avatar, date
- BoardColumn component (`src/components/board-column.tsx`) — Kanban column with header (status icon, name, count, add button) and scrollable card area
- Team Board page (`src/app/(app)/team/[key]/board/page.tsx`) — full Kanban board:
  - Fetches issues from same API as list view (`/api/teams/[key]/issues`)
  - Columns per workflow state with issue cards
  - Hides empty completed/canceled columns by default
  - Empty state fallback
  - Header with team name and issue count
- Updated empty states test for board page (mocked fetch)

### Test results
- 11 unit tests passing (tests/board-view.test.tsx)
  - 7 IssueCard tests (identifier, title, priority, labels, assignee, date, no-assignee)
  - 4 BoardColumn tests (header, status icon, children, add button)
- 209 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/components/issue-card.tsx — new IssueCard component
- src/components/board-column.tsx — new BoardColumn component
- src/app/(app)/team/[key]/board/page.tsx — rewritten with Kanban board
- tests/board-view.test.tsx — new unit test file (11 tests)
- tests/empty-states.test.tsx — updated board test with fetch mock
- prd.json — marked feature-002 build_pass: true
- qa-hints.json — added feature-002 QA hints

### Decisions
- Reused same API route as list view (/api/teams/[key]/issues) — both views show same data, different layout
- Drag & drop not implemented in this pass (will need a DnD library like @dnd-kit)
- Empty completed/canceled columns hidden by default to match Linear's behavior
- IssueCard is separate from IssueRow — different layout needs (card vs row)

## Iteration 12 — feature-003: Issue Detail Page
Date: 2026-04-07

### What was built
- API route `GET /api/issues/[id]` — fetches full issue detail by identifier or UUID, with state, assignee, creator, team, project, labels, and comments (all fetched in parallel)
- IssueProperties component (`src/components/issue-properties.tsx`) — properties sidebar with Status, Priority, Assignee, Labels, Project rows
- Issue detail page (`src/app/(app)/issue/[id]/page.tsx`) — full detail view:
  - Header with identifier and team name
  - Large title display
  - Description area (plain text, placeholder for rich text)
  - Properties sidebar (status, priority, assignee, labels, project)
  - Activity feed with comments
  - Comment composer textarea
  - Created/Updated metadata

### Test results
- 10 unit tests passing (tests/issue-detail.test.tsx)
  - 10 IssueProperties tests (status, priority, assignee, labels, project, property labels)
- 219 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/api/issues/[id]/route.ts — new API route
- src/components/issue-properties.tsx — new IssueProperties component
- src/app/(app)/issue/[id]/page.tsx — new issue detail page
- tests/issue-detail.test.tsx — new unit test file (10 tests)
- prd.json — marked feature-003 build_pass: true
- qa-hints.json — added feature-003 QA hints

### Decisions
- Used `/issue/[id]` URL pattern (matching Linear's actual URLs) rather than `/team/{key}/issue/{id}`
- API supports lookup by identifier (ENG-45) or UUID
- Related data fetched in parallel with Promise.all for performance
- Description is plain text for now — rich text editing will be a future enhancement
- Comment composer is a placeholder textarea — submission not wired up yet

## Iteration 13 — feature-004: Create Issue Modal
Date: 2026-04-07

### What was built
- API route `POST /api/issues` — creates new issue with auto-generated identifier (ENG-N), supports title, description, team, state, priority, assignee, project
- CreateIssueModal component (`src/components/create-issue-modal.tsx`) — modal with:
  - Team selector header (ENG badge + "New issue" breadcrumb)
  - Title input and description textarea
  - Bottom toolbar: Status (Backlog), Priority, Labels buttons
  - More actions button
  - Footer: "Create more" toggle + "Create Issue" submit button
  - Submit disabled when title empty, submits via POST /api/issues
  - Close button and backdrop click to dismiss

### Test results
- 12 unit tests passing (tests/create-issue-modal.test.tsx)
- 231 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/api/issues/route.ts — new POST API route
- src/components/create-issue-modal.tsx — new CreateIssueModal component
- tests/create-issue-modal.test.tsx — new unit test file (12 tests)
- prd.json — marked feature-004 build_pass: true
- qa-hints.json — added feature-004 QA hints

### Decisions
- Auto-increments issue number per team using MAX(number) + 1
- Defaults to backlog workflow state if no stateId provided
- Toolbar buttons are visual placeholders — dropdown menus will be added when filter/combobox infrastructure is built
- Removed autoFocus to comply with Biome accessibility rules

## Iteration 14 — feature-005: Display Options Panel
Date: 2026-04-07

### What was built
- Enhanced DisplayOptionsPanel component (`src/components/display-options-panel.tsx`) with:
  - List/Board layout toggle (navigates between /team/{key}/all and /team/{key}/board)
  - Interactive Grouping selector (Status, Priority, Assignee, Label, Project, None)
  - Interactive Sub-group selector (None, Status, Priority, Assignee)
  - Interactive Ordering selector (Priority, Created, Updated, Manual)
  - 13 toggleable display properties (ID, Status, Assignee, Priority, Project, Due date, Milestone, Labels, Links, Time in status, Created, Updated, Pull requests)
  - Show sub-issues toggle, Show triage issues toggle, Show empty columns toggle
  - Reset button to restore defaults
  - "Set default for everyone" button to save as team default via API
- API route `GET/PUT /api/teams/[key]/display-options` — reads/writes display options to team.settings JSONB
- useDisplayOptions hook (`src/hooks/use-display-options.ts`) — manages display options state, loads from API, provides save/reset functions
- Integrated display options panel into both Team Issues (list) and Team Board pages
- IssueRow component updated to respect displayProperties — conditionally shows/hides ID, Status, Priority, Assignee, Labels, Created date

### Test results
- 20 unit tests passing (tests/display-options.test.tsx)
  - Panel open/close, layout toggle, grouping/sub-group/ordering selectors
  - Display property chips, toggle callbacks
  - Show sub-issues, Show triage, Board options toggles
  - Reset and Save as default button callbacks
  - defaultDisplayProperties shape validation
- 251 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/components/display-options-panel.tsx — enhanced with interactive selectors, exported types
- src/app/api/teams/[key]/display-options/route.ts — new GET/PUT API route
- src/hooks/use-display-options.ts — new custom hook for display options state
- src/app/(app)/team/[key]/all/page.tsx — integrated display options panel + button
- src/app/(app)/team/[key]/board/page.tsx — integrated display options panel + button
- src/components/issue-row.tsx — added displayProperties prop for conditional rendering
- tests/display-options.test.tsx — updated with 20 tests for new API
- tests/empty-states.test.tsx — added useRouter mock for compatibility
- prd.json — marked feature-005 build_pass: true
- qa-hints.json — added feature-005 QA hints

### Decisions
- Display options stored in team.settings JSONB field (no schema migration needed)
- Layout toggle navigates between /team/{key}/all and /team/{key}/board via router.push
- InlineSelect component uses data-testid for dropdown menus to allow scoped testing
- IssueRow conditionally renders elements based on displayProperties; defaults to showing all if no prop passed (backwards compatible)
- useDisplayOptions hook loads saved options from API on mount, overrides layout with current page's layout

## Iteration 15 — feature-006: Filter Bar
Date: 2026-04-07

### What was built
- FilterBar component (`src/components/filter-bar.tsx`) with:
  - "Filter" button that opens a dropdown of filter types (Status, Priority, Assignee, Label, Project, Creator, Due date)
  - Each filter type opens a value picker with checkboxes
  - Active filters shown as removable chips with resolved value labels
  - Clear all button to remove all active filters
  - Multiple values per filter type (OR within, AND across filters)
  - `isNot` operator support for negation
  - Icons for each filter type
- `applyFilters` utility function for client-side issue filtering
  - Filters by status, priority, assignee, label, project
  - AND logic across different filter types, OR within same type
  - isNot operator inverts match
- `useFilters` hook (`src/hooks/use-filters.ts`) for filter state management
- Updated issues API to return `stateId`, `assigneeId`, `labelIds`, `projectId` per issue and `filterOptions` (statuses, assignees, labels, priorities)
- Integrated FilterBar into both Team Issues list and Team Board pages
- Filters apply client-side to issue groups using `useMemo`

### Test results
- 23 unit tests passing (tests/filter-bar.test.tsx)
  - 15 FilterBar component tests (render, menus, add/remove filters, chips, clear)
  - 8 applyFilters utility tests (all filter types, AND/OR logic, isNot)
- 274 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/components/filter-bar.tsx — new FilterBar component + applyFilters utility
- src/hooks/use-filters.ts — new useFilters hook
- src/app/api/teams/[key]/issues/route.ts — added stateId/assigneeId/labelIds/projectId to response, added filterOptions
- src/app/(app)/team/[key]/all/page.tsx — integrated FilterBar with client-side filtering
- src/app/(app)/team/[key]/board/page.tsx — integrated FilterBar with client-side filtering
- tests/filter-bar.test.tsx — new unit test file (23 tests)
- prd.json — marked feature-006 build_pass: true
- qa-hints.json — added feature-006 QA hints

### Decisions
- Client-side filtering using applyFilters utility (no server-side filter query params needed for current scale)
- Filter options extracted from API response (unique assignees/labels from current issues)
- Filters use AND logic across types, OR within same type (matching Linear behavior)
- FilterBar is a controlled component — parent manages filter state via useFilters hook
- SVG icons use aria-hidden with title elements to satisfy Biome accessibility rules

## Iteration 16 — feature-010: Projects List Page
Date: 2026-04-07

### What was built
- API route `GET /api/projects` — fetches all projects for user's workspace with lead info and progress calculation
- ProjectRow component (`src/components/project-row.tsx`) — table row with:
  - Emoji icon + project name (clickable link to /project/{slug}/overview)
  - Health status text
  - Priority icon
  - Lead avatar
  - Target date (formatted)
  - Progress ring SVG with percentage
- ProjectStatusBadge component (`src/components/project-status-badge.tsx`) — colored badge for planned/started/paused/completed/canceled
- Projects page (`src/app/(app)/projects/page.tsx`) — full projects table:
  - Header with "Projects" title and "All projects" tab
  - Table columns: Name, Health, Priority, Lead, Target date, Status (progress %)
  - Empty state fallback when no projects
  - Footer with project count

### Test results
- 15 unit tests passing (tests/projects-list.test.tsx)
  - 10 ProjectRow tests (name, icon, health, lead, progress, date, priority, link, 0%, 100%)
  - 5 ProjectStatusBadge tests (all statuses)
- 289 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/api/projects/route.ts — new API route
- src/components/project-row.tsx — new ProjectRow component
- src/components/project-status-badge.tsx — new ProjectStatusBadge component
- src/app/(app)/projects/page.tsx — rewritten with projects table
- tests/projects-list.test.tsx — new unit test file (15 tests)
- tests/empty-states.test.tsx — updated Projects test with fetch mock
- prd.json — marked feature-010 build_pass: true
- qa-hints.json — added feature-010 QA hints

### Decisions
- Progress calculated from issue completedAt counts per project (server-side)
- Health hardcoded as "No updates" for now (matches Linear's default when no project updates exist)
- Project rows link to /project/{slug}/overview for future detail page
- Progress ring uses SVG circles with stroke-dasharray for smooth rendering

## Iteration 17 — feature-011: Project Detail Page
Date: 2026-04-07

### What was built
- API route `GET /api/projects/[slug]` — fetches full project detail by slug with lead, members, teams, milestones, issues (grouped by state), and progress
- ProjectProperties component (`src/components/project-properties.tsx`) — properties sidebar with Status, Priority, Lead, Members, Dates, Teams, Labels
- MilestoneRow component (`src/components/milestone-row.tsx`) — milestone with name, progress bar, percentage, and issue count
- Project detail page (`src/app/(app)/project/[slug]/page.tsx`) — full detail view:
  - Overview/Activity/Issues tabs
  - Large emoji icon + title + description
  - Properties sidebar
  - Resources section with add CTA
  - "Write first project update" CTA
  - Milestones list with progress bars
  - Progress summary (completed/total)
  - Issues tab with issues grouped by workflow state

### Test results
- 12 unit tests passing (tests/project-detail.test.tsx)
  - 8 ProjectProperties tests (status, priority, lead, teams, dates, labels)
  - 4 MilestoneRow tests (name, progress, count, bar)
- 301 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/api/projects/[slug]/route.ts — new API route
- src/components/project-properties.tsx — new ProjectProperties component
- src/components/milestone-row.tsx — new MilestoneRow component
- src/app/(app)/project/[slug]/page.tsx — new project detail page
- tests/project-detail.test.tsx — new unit test file (12 tests)
- prd.json — marked feature-011 build_pass: true
- qa-hints.json — added feature-011 QA hints

### Decisions
- Related data (lead, milestones, teams, members, issues) fetched in parallel with Promise.all
- Issues grouped by workflow state for Issues tab, reusing IssueRow/IssuesGroupHeader components
- Milestones show progress based on all project issues (simplified; real Linear tracks per-milestone issues)
- Activity tab is placeholder text for now

## Iteration 18 — feature-020: Inbox Page (Notifications)
Date: 2026-04-07

### What was built
- NotificationRow component (`src/components/notification-row.tsx`) — renders actor avatar, name, action description, issue identifier + title, unread dot indicator, relative timestamp, selected state
  - 5 notification types: assigned, mentioned, status_change, comment, duplicate
  - Relative time formatting (now, Xm, Xh, Xd, Xmo)
- API route `GET /api/notifications` — fetches user's notifications with actor and issue info, returns unread count
- API route `PATCH /api/notifications/[id]/read` — marks a notification as read (validates ownership)
- Inbox page (`src/app/(app)/inbox/page.tsx`) — full inbox view:
  - Header with "Inbox" title and unread count
  - Notification list (left panel, 400px on desktop)
  - Detail panel (right side, desktop only) showing selected notification info
  - Mark as read on click (optimistic UI update + API call)
  - Empty state fallback ("You're all caught up")
  - Loading state
- Updated empty-states test to mock fetch for Inbox page

### Test results
- 17 unit tests passing (tests/inbox.test.tsx)
  - 14 NotificationRow tests (actor name, identifier, title, 5 action types, unread dot, read state, avatar initials, onClick, selected style, relative timestamp)
  - 3 Inbox page tests (empty state, notification list, header with unread count)
- 318 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/components/notification-row.tsx — new NotificationRow component
- src/app/api/notifications/route.ts — new GET API route
- src/app/api/notifications/[id]/read/route.ts — new PATCH API route
- src/app/(app)/inbox/page.tsx — rewritten with notification list + detail panel
- tests/empty-states.test.tsx — updated Inbox test with fetch mock
- prd.json — marked feature-020 build_pass: true
- qa-hints.json — added feature-020 QA hints

### Decisions
- Used optimistic UI for mark-as-read (update state immediately, fire API in background)
- Detail panel is desktop-only (hidden on mobile, full-width list)
- Notifications limited to 100 most recent for performance
- Reused Avatar component from design system for actor avatars
- Filter/sort buttons not yet implemented (noted in QA hints)

## Iteration 19 — feature-021: My Issues Page
Date: 2026-04-07

### What was built
- API route `GET /api/my-issues` — fetches issues for current user across all workspace teams
  - Tab support: `assigned` (by assigneeId), `created` (by creatorId), `subscribed` (commented issues), `activity` (merged assigned + created)
  - Returns issues grouped by workflow state with labels, assignees, and filter options
- My Issues page (`src/app/(app)/my-issues/[tab]/page.tsx`) — full page with:
  - 4 tabs: Assigned, Created, Subscribed, Activity (URL-driven via dynamic route)
  - Issues grouped by workflow state with IssueRow and IssuesGroupHeader components
  - FilterBar integration with client-side filtering
  - Display options panel with toggleable display properties
  - Hidden issues count when filters are active
  - Empty state per tab with contextual message
  - Footer with issue count
- Base `/my-issues` page redirects to `/my-issues/assigned`
- Updated sidebar link to `/my-issues/assigned`

### Test results
- 13 unit tests passing (tests/my-issues.test.tsx)
  - Heading, 4 tabs, identifiers, titles, group headers, issue count, labels, empty state, display/filter buttons, fetch URL, tab navigation, assignee avatars
- 14 empty-states tests passing (updated for new MyIssuesTabPage import)
- 331 total tests passing (excluding pre-existing onboard-validation failures)
- TypeScript typecheck: clean
- Biome lint/format: clean

### Files changed
- src/app/api/my-issues/route.ts — new API route
- src/app/(app)/my-issues/[tab]/page.tsx — new My Issues tab page
- src/app/(app)/my-issues/page.tsx — rewritten as redirect to /my-issues/assigned
- src/components/sidebar.tsx — updated My Issues link to /my-issues/assigned
- tests/my-issues.test.tsx — new unit test file (13 tests)
- tests/empty-states.test.tsx — updated import to MyIssuesTabPage
- prd.json — marked feature-021 build_pass: true
- qa-hints.json — added feature-021 QA hints

### Decisions
- Used dynamic route `/my-issues/[tab]` for clean tab URLs matching Linear's pattern
- Subscribed tab uses commented issues as proxy (no dedicated subscription table)
- Activity tab merges assigned + created issues (simplified from Linear's chronological activity)
- Reused IssueRow, IssuesGroupHeader, FilterBar, and design system components
- Display options are local state (not persisted to API like team display options)
