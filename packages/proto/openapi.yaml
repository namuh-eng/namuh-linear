openapi: 3.1.0
info:
  title: Exponential Headless API
  version: 0.1.0
jsonSchemaDialect: https://json-schema.org/draft/2020-12/schema
servers:
  - url: http://localhost:3016/v1
security:
  - bearerAuth: []
paths:
  /issues:
    get:
      operationId: listIssues
      summary: List issues in the authenticated workspace
      parameters:
        - $ref: '#/components/parameters/Cursor'
        - $ref: '#/components/parameters/Limit'
        - name: team_id
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Issues page
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createIssue
      summary: Create an issue
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateIssueRequest'
      responses:
        '201':
          description: Created issue
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Issue'
        default:
          $ref: '#/components/responses/Problem'
  /issues/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: getIssue
      responses:
        '200':
          description: Issue
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Issue'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateIssue
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateIssueRequest'
      responses:
        '200':
          description: Updated issue
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Issue'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteIssue
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                type: object
                required: [success]
                properties:
                  success: { type: boolean }
        default:
          $ref: '#/components/responses/Problem'
  /issues/{id}/comments:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    post:
      operationId: createIssueComment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCommentRequest'
      responses:
        '201':
          description: Created comment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Comment'
        default:
          $ref: '#/components/responses/Problem'
  /issues/{id}/reactions:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    post:
      operationId: toggleIssueReaction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReactionRequest'
      responses:
        '200':
          description: Issue reaction summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReactionSummaryList'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteIssueReaction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReactionRequest'
      responses:
        '200':
          description: Issue reaction summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReactionSummaryList'
        default:
          $ref: '#/components/responses/Problem'
  /issues/bulk:
    patch:
      operationId: bulkUpdateIssues
      summary: Bulk update issues in the authenticated workspace
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkUpdateIssuesRequest'
      responses:
        '200':
          description: Bulk update result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkUpdateIssuesResponse'
        default:
          $ref: '#/components/responses/Problem'
  /issues/search:
    get:
      operationId: searchIssues
      parameters:
        - name: q
          in: query
          schema: { type: string }
        - name: workspaceId
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Issue search results
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/IssueSearchResult' }
        default:
          $ref: '#/components/responses/Problem'
  /issues/{id}/subscription:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: getIssueSubscription
      responses:
        '200':
          description: Issue subscription summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueSubscriptionSummary'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: subscribeToIssue
      responses:
        '200':
          description: Issue subscription summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueSubscriptionSummary'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: unsubscribeFromIssue
      responses:
        '200':
          description: Issue subscription summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueSubscriptionSummary'
        default:
          $ref: '#/components/responses/Problem'
  /issue-templates:
    get:
      operationId: listIssueTemplates
      parameters:
        - name: teamKey
          in: query
          schema: { type: string }
      responses:
        '200':
          description: Issue templates
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueTemplateListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createIssueTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateIssueTemplateRequest'
      responses:
        '201':
          description: Created issue template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueTemplateResponse'
        default:
          $ref: '#/components/responses/Problem'
  /issue-templates/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: updateIssueTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateIssueTemplateRequest'
      responses:
        '200':
          description: Updated issue template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueTemplateResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteIssueTemplate
      responses:
        '200':
          description: Deleted issue template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /account:
    get:
      operationId: accountNamespace
      summary: Account route namespace; concrete resources are under /account/profile and /account/preferences.
      responses:
        default:
          $ref: '#/components/responses/Problem'
  /account/profile:
    get:
      operationId: getAccountProfile
      responses:
        '200':
          description: Account profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountProfileResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateAccountProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAccountProfileRequest'
      responses:
        '200':
          description: Updated account profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountProfileResponse'
        default:
          $ref: '#/components/responses/Problem'
  /account/notifications:
    get:
      operationId: getAccountNotifications
      responses:
        '200':
          description: Account notification settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountNotificationsResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateAccountNotifications
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAccountNotificationsRequest'
      responses:
        '200':
          description: Updated account notification settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountNotificationsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /account/profile/workspace:
    delete:
      operationId: leaveAccountWorkspace
      responses:
        '200':
          description: Left active workspace
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaveWorkspaceResponse'
        default:
          $ref: '#/components/responses/Problem'
  /account/preferences:
    get:
      operationId: getAccountPreferences
      responses:
        '200':
          description: Account preferences
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountPreferencesResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateAccountPreferences
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAccountPreferencesRequest'
      responses:
        '200':
          description: Updated account preferences
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AccountPreferencesResponse'
        default:
          $ref: '#/components/responses/Problem'
  /agent/runs:
    get:
      operationId: listAgentRuns
      summary: List workspace agent runs
      responses:
        '200':
          description: Agent runs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createAgentRun
      summary: Create an agent run
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAgentRunRequest'
      responses:
        '201':
          description: Created agent run
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentRunResponse'
        default:
          $ref: '#/components/responses/Problem'
  /analytics:
    get:
      operationId: analyticsNamespace
      summary: Analytics route namespace; concrete resources are under /analytics/workspace.
      responses:
        default:
          $ref: '#/components/responses/Problem'
  /analytics/workspace:
    get:
      operationId: getWorkspaceAnalytics
      responses:
        '200':
          description: Workspace analytics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceAnalyticsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /auth:
    get:
      operationId: authNamespace
      summary: Auth route namespace; concrete resources are under /auth/provider-capabilities.
      responses:
        default:
          $ref: '#/components/responses/Problem'
  /auth/saml/discovery:
    post:
      operationId: discoverSamlUrl
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SamlDiscoveryRequest'
      responses:
        '200':
          description: Discovered SAML URL
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SamlDiscoveryResponse'
        default:
          $ref: '#/components/responses/Problem'
  /auth/provider-capabilities:
    get:
      operationId: getAuthProviderCapabilities
      parameters:
        - name: callbackUrl
          in: query
          schema: { type: string }
      responses:
        '200':
          description: Auth provider capabilities
          headers:
            Cache-Control:
              schema: { type: string }
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthProviderCapabilitiesResponse'
        default:
          $ref: '#/components/responses/Problem'
  /comments/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: updateComment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCommentRequest'
      responses:
        '200':
          description: Updated comment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Comment'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteComment
      responses:
        '200':
          description: Deleted comment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /comments/{id}/reactions:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      operationId: toggleCommentReaction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReactionRequest'
      responses:
        '200':
          description: Comment reaction summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReactionSummaryList'
        default:
          $ref: '#/components/responses/Problem'
  /custom-emojis:
    get:
      operationId: listCustomEmojis
      responses:
        '200':
          description: Custom emojis
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomEmojiListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createCustomEmoji
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCustomEmojiRequest'
      responses:
        '201':
          description: Created custom emoji
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomEmojiResponse'
        default:
          $ref: '#/components/responses/Problem'
  /custom-emojis/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    delete:
      operationId: deleteCustomEmoji
      responses:
        '200':
          description: Deleted custom emoji
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        default:
          $ref: '#/components/responses/Problem'
  /document-settings:
    get:
      operationId: getDocumentSettings
      responses:
        '200':
          description: Document settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentSettingsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /document-folders:
    post:
      operationId: createDocumentFolder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentFolderRequest'
      responses:
        '201':
          description: Created document folder
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentFolderResponse'
        default:
          $ref: '#/components/responses/Problem'
  /document-folders/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    patch:
      operationId: updateDocumentFolder
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentFolderRequest'
      responses:
        '200':
          description: Updated document folder
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentFolderResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteDocumentFolder
      responses:
        '200':
          description: Deleted document folder
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /document-templates:
    post:
      operationId: createDocumentTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentTemplateRequest'
      responses:
        '201':
          description: Created document template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentTemplateResponse'
        default:
          $ref: '#/components/responses/Problem'
  /document-templates/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    patch:
      operationId: updateDocumentTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentTemplateRequest'
      responses:
        '200':
          description: Updated document template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentTemplateResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteDocumentTemplate
      responses:
        '200':
          description: Deleted document template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /inbound:
    get:
      operationId: inboundNamespace
      summary: Inbound webhook namespace; concrete resources are under /inbound/team-email.
      responses:
        default:
          $ref: '#/components/responses/Problem'
  /inbound/team-email:
    post:
      operationId: createInboundTeamEmailIssue
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InboundTeamEmailRequest'
      responses:
        '201':
          description: Created issue from inbound team email
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InboundTeamEmailResponse'
        default:
          $ref: '#/components/responses/Problem'
  /integrations:
    get:
      operationId: listIntegrations
      responses:
        '200':
          description: Workspace integrations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IntegrationListResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteIntegration
      parameters:
        - name: provider
          in: query
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Deleted integration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /integrations/slack/connect:
    post:
      operationId: connectSlackIntegration
      responses:
        '200':
          description: Slack authorization URL
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SlackConnectResponse'
        '412':
          description: Slack OAuth is not configured
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SlackConfigurationRequiredResponse'
        default:
          $ref: '#/components/responses/Problem'
  /integrations/slack/disconnect:
    post:
      operationId: disconnectSlackIntegration
      responses:
        '200':
          description: Slack disconnected
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /labels:
    get:
      operationId: listLabels
      parameters:
        - name: scope
          in: query
          schema: { type: string, enum: [workspace, team, all], default: workspace }
        - name: teamId
          in: query
          schema: { type: string, format: uuid }
        - name: includeArchived
          in: query
          schema: { type: boolean, default: false }
      responses:
        '200':
          description: Labels
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LabelListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createLabel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateLabelRequest'
      responses:
        '201':
          description: Created label
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LabelResponse'
        default:
          $ref: '#/components/responses/Problem'
  /labels/bulk:
    post:
      operationId: bulkLabelAction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BulkLabelRequest'
      responses:
        '200':
          description: Bulk label action result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkLabelResponse'
        default:
          $ref: '#/components/responses/Problem'
  /labels/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: updateLabel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateLabelRequest'
      responses:
        '200':
          description: Updated label
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LabelResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteLabel
      responses:
        '200':
          description: Deleted label
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-labels:
    get:
      operationId: listProjectLabels
      responses:
        '200':
          description: Project labels
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectLabelListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createProjectLabel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectLabelRequest'
      responses:
        '201':
          description: Created project label
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectLabelResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-labels/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: updateProjectLabel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectLabelRequest'
      responses:
        '200':
          description: Updated project label
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectLabelResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteProjectLabel
      responses:
        '200':
          description: Deleted project label
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /my-issues:
    get:
      operationId: listMyIssues
      summary: List issues related to the authenticated user
      parameters:
        - name: tab
          in: query
          schema:
            type: string
            enum: [assigned, created, subscribed, activity]
      responses:
        '200':
          description: Grouped my issues
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyIssuesResponse'
        default:
          $ref: '#/components/responses/Problem'
  /notifications:
    get:
      operationId: listNotifications
      responses:
        '200':
          description: Notifications
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationListResponse'
        default:
          $ref: '#/components/responses/Problem'
  /notifications/bulk-read:
    patch:
      operationId: markNotificationsRead
      responses:
        '200':
          description: Bulk mark notifications read
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkReadNotificationsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /notifications/{id}/read:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: markNotificationRead
      responses:
        '200':
          description: Notification read result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationActionResponse'
        default:
          $ref: '#/components/responses/Problem'
  /notifications/{id}/unread:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: markNotificationUnread
      responses:
        '200':
          description: Notification unread result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationActionResponse'
        default:
          $ref: '#/components/responses/Problem'
  /notifications/{id}/snooze:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: snoozeNotification
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SnoozeNotificationRequest'
      responses:
        '200':
          description: Notification snooze result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NotificationActionResponse'
        default:
          $ref: '#/components/responses/Problem'
  /personal-access-tokens:
    get:
      operationId: listPersonalAccessTokens
      responses:
        '200':
          description: Personal access tokens
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonalAccessTokenListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createPersonalAccessToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePersonalAccessTokenRequest'
      responses:
        '201':
          description: Created personal access token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatePersonalAccessTokenResponse'
        default:
          $ref: '#/components/responses/Problem'
  /personal-access-tokens/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    delete:
      operationId: revokePersonalAccessToken
      responses:
        '200':
          description: Revoked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-templates:
    get:
      operationId: listProjectTemplates
      summary: List project templates in the authenticated workspace
      responses:
        '200':
          description: Project templates
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTemplateListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createProjectTemplate
      summary: Create a project template
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectTemplateRequest'
      responses:
        '201':
          description: Created project template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTemplateResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-templates/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: updateProjectTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectTemplateRequest'
      responses:
        '200':
          description: Updated project template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTemplateResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteProjectTemplate
      responses:
        '200':
          description: Deleted project template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-statuses:
    get:
      operationId: listProjectStatuses
      summary: List configurable project statuses for the authenticated workspace
      responses:
        '200':
          description: Project statuses
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectStatusListResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateProjectStatuses
      summary: Replace configurable project statuses for the authenticated workspace
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectStatusesRequest'
      responses:
        '200':
          description: Updated project statuses
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectStatusListResponse'
        default:
          $ref: '#/components/responses/Problem'
  /sidebar:
    get:
      operationId: sidebarNamespace
      summary: Sidebar route namespace; concrete resources are under /sidebar/favorites.
      responses:
        default:
          $ref: '#/components/responses/Problem'
  /sidebar/favorites:
    get:
      operationId: listSidebarFavorites
      responses:
        '200':
          description: Sidebar favorites
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SidebarFavoriteListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: addSidebarFavorite
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SidebarFavoriteRequest'
      responses:
        '201':
          description: Sidebar favorites
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SidebarFavoriteListResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: reorderSidebarFavorites
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderSidebarFavoritesRequest'
      responses:
        '200':
          description: Sidebar favorites
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SidebarFavoriteListResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: removeSidebarFavorite
      parameters:
        - name: objectType
          in: query
          required: true
          schema: { $ref: '#/components/schemas/SidebarFavoriteObjectType' }
        - name: objectId
          in: query
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Sidebar favorites
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SidebarFavoriteListResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-update-configurations:
    get:
      operationId: listProjectUpdateConfigurations
      summary: List project update configurations
      responses:
        '200':
          description: Project update configurations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectUpdateConfigurationListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createProjectUpdateConfiguration
      summary: Create a project update configuration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectUpdateConfigurationRequest'
      responses:
        '201':
          description: Created project update configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectUpdateConfigurationResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-update-configurations/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    patch:
      operationId: updateProjectUpdateConfiguration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectUpdateConfigurationRequest'
      responses:
        '200':
          description: Updated project update configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectUpdateConfigurationResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteProjectUpdateConfiguration
      responses:
        '200':
          description: Deleted project update configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-updates:
    get:
      operationId: listProjectUpdates
      summary: List project update reminder configurations
      responses:
        '200':
          description: Project update reminder configurations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectUpdateListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createProjectUpdate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectUpdateRequest'
      responses:
        '201':
          description: Created project update reminder configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectUpdateResponse'
        default:
          $ref: '#/components/responses/Problem'
  /project-updates/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    patch:
      operationId: updateProjectUpdate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectUpdateRequest'
      responses:
        '200':
          description: Updated project update reminder configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectUpdateResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteProjectUpdate
      responses:
        '200':
          description: Deleted project update reminder configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /projects:
    get:
      operationId: listProjects
      summary: List projects in the authenticated workspace
      responses:
        '200':
          description: Projects
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createProject
      summary: Create a project
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectRequest'
      responses:
        '201':
          description: Created project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        default:
          $ref: '#/components/responses/Problem'
  /projects/{slug}:
    parameters:
      - name: slug
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: getProject
      responses:
        '200':
          description: Project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateProject
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectRequest'
      responses:
        '200':
          description: Updated project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Project'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteProject
      responses:
        '200':
          description: Deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /projects/{slug}/milestones:
    parameters:
      - name: slug
        in: path
        required: true
        schema: { type: string }
    post:
      operationId: createProjectMilestone
      summary: Create a project milestone
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectMilestoneRequest'
      responses:
        '201':
          description: Created milestone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectMilestoneResponse'
        default:
          $ref: '#/components/responses/Problem'
  /projects/{slug}/milestones/{milestoneID}:
    parameters:
      - name: slug
        in: path
        required: true
        schema: { type: string }
      - name: milestoneID
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: updateProjectMilestone
      summary: Update a project milestone
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectMilestoneRequest'
      responses:
        '200':
          description: Updated milestone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectMilestoneResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteProjectMilestone
      summary: Delete a project milestone
      responses:
        '200':
          description: Deleted milestone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /teams:
    get:
      operationId: listTeams
      summary: List visible teams in the authenticated workspace
      responses:
        '200':
          description: Teams
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createTeam
      summary: Create a team with default workflow states
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTeamRequest'
      responses:
        '201':
          description: Created team
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateTeamResponse'
        default:
          $ref: '#/components/responses/Problem'
  /teams/{key}/context:
    parameters:
      - name: key
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: getTeamContext
      responses:
        '200':
          description: Team navigation context
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamContextResponse'
        default:
          $ref: '#/components/responses/Problem'
  /teams/{key}/issues:
    parameters:
      - name: key
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: getTeamIssues
      responses:
        '200':
          description: Team issues grouped by workflow state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamIssuesResponse'
        default:
          $ref: '#/components/responses/Problem'
  /teams/{key}/display-options:
    parameters:
      - name: key
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: getTeamDisplayOptions
      responses:
        '200':
          description: Team display options
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamDisplayOptionsResponse'
        default:
          $ref: '#/components/responses/Problem'
    put:
      operationId: updateTeamDisplayOptions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                displayOptions: {}
      responses:
        '200':
          description: Updated team display options
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamDisplayOptionsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /teams/{key}/cycles:
    parameters:
      - name: key
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: listTeamCycles
      responses:
        '200':
          description: Team cycles
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeamCyclesResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createTeamCycle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCycleRequest'
      responses:
        '201':
          description: Created cycle
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cycle'
        default:
          $ref: '#/components/responses/Problem'
  /teams/{key}/create-issue-options:
    parameters:
      - name: key
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: getTeamCreateIssueOptions
      responses:
        '200':
          description: Team create issue options
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateIssueOptionsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /teams/{key}/cycles/{cycle_id}:
    parameters:
      - name: key
        in: path
        required: true
        schema: { type: string }
      - name: cycle_id
        in: path
        required: true
        schema: { type: string, format: uuid }
    patch:
      operationId: updateTeamCycle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCycleRequest'
      responses:
        '200':
          description: Updated cycle
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cycle'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteTeamCycle
      responses:
        '200':
          description: Deleted cycle
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /views:
    get:
      operationId: listViews
      summary: List custom views in the authenticated workspace
      responses:
        '200':
          description: Custom views and available teams
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ViewListResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createView
      summary: Create a custom view
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ViewRequest'
      responses:
        '201':
          description: Created custom view
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ViewResponse'
        default:
          $ref: '#/components/responses/Problem'
  /views/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      operationId: getView
      summary: Get a custom view
      responses:
        '200':
          description: Custom view
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ViewResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateView
      summary: Update a custom view
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ViewRequest'
      responses:
        '200':
          description: Updated custom view
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ViewResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteView
      summary: Delete a custom view
      responses:
        '200':
          description: Deleted custom view
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces:
    get:
      operationId: listWorkspaces
      summary: List workspaces for the authenticated principal
      responses:
        '200':
          description: Workspace memberships
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/WorkspaceMembership' }
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createWorkspace
      summary: Create a workspace with a default team and workflow states
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWorkspaceRequest'
      responses:
        '201':
          description: Created workspace
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateWorkspaceResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/security/saml:
    patch:
      operationId: updateCurrentWorkspaceSaml
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SamlSettingsPatch'
      responses:
        '200':
          description: Updated SAML settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SamlSettingsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/security/scim:
    patch:
      operationId: updateCurrentWorkspaceScim
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [enabled]
              properties:
                enabled: { type: boolean }
      responses:
        '200':
          description: Updated SCIM settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScimSettingsResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createCurrentWorkspaceScimToken
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                name: { type: string }
      responses:
        '200':
          description: Created SCIM token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateScimTokenResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/security/scim/tokens/{tokenId}:
    parameters:
      - name: tokenId
        in: path
        required: true
        schema: { type: string }
    delete:
      operationId: revokeCurrentWorkspaceScimToken
      responses:
        '200':
          description: Revoked SCIM token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScimSettingsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/applications:
    get:
      operationId: listCurrentWorkspaceApplications
      responses:
        '200':
          description: Current workspace authorized applications
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceApplicationsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/applications/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    delete:
      operationId: deleteCurrentWorkspaceApplication
      responses:
        '200':
          description: Deleted authorized application
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/sla:
    get:
      operationId: getCurrentWorkspaceSla
      responses:
        '200':
          description: Current workspace SLA settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceSlaResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createCurrentWorkspaceSlaPolicy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SlaPolicyInput'
      responses:
        '201':
          description: Created SLA policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SlaPolicyResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/sla/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema: { type: string }
    patch:
      operationId: updateCurrentWorkspaceSlaPolicy
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SlaPolicyInput'
      responses:
        '200':
          description: Updated SLA policy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SlaPolicyResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteCurrentWorkspaceSlaPolicy
      responses:
        '200':
          description: Deleted SLA policy
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok: { type: boolean }
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/ai-settings:
    get:
      operationId: getCurrentWorkspaceAiSettings
      responses:
        '200':
          description: Current workspace AI settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceAiSettingsResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateCurrentWorkspaceAiSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkspaceAiSettingsRequest'
      responses:
        '200':
          description: Updated AI settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceAiSettingsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/initiatives-settings:
    get:
      operationId: getCurrentWorkspaceInitiativeSettings
      responses:
        '200':
          description: Current workspace initiative settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceInitiativeSettingsResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateCurrentWorkspaceInitiativeSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkspaceInitiativeSettingsRequest'
      responses:
        '200':
          description: Updated initiative settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceInitiativeSettingsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/collaboration:
    get:
      operationId: getCurrentWorkspaceCollaboration
      responses:
        '200':
          description: Current workspace collaboration settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceCollaborationResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateCurrentWorkspaceCollaboration
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkspaceCollaborationRequest'
      responses:
        '200':
          description: Updated collaboration settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceCollaborationResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/documents:
    get:
      operationId: getCurrentWorkspaceDocuments
      responses:
        '200':
          description: Current workspace document settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceDocumentsResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateCurrentWorkspaceDocuments
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkspaceDocumentsRequest'
      responses:
        '200':
          description: Updated document settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceDocumentsResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: createCurrentWorkspaceDocumentTemplate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWorkspaceDocumentTemplateRequest'
      responses:
        '201':
          description: Created document template
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceDocumentsResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current/billing:
    get:
      operationId: getCurrentWorkspaceBilling
      responses:
        '200':
          description: Current workspace billing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceBillingResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateCurrentWorkspaceBilling
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkspaceBillingRequest'
      responses:
        '200':
          description: Updated workspace billing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceBillingResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/current:
    get:
      operationId: getCurrentWorkspace
      responses:
        '200':
          description: Current workspace
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentWorkspaceResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateCurrentWorkspace
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWorkspaceRequest'
      responses:
        '200':
          description: Updated current workspace
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentWorkspaceResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: deleteCurrentWorkspace
      responses:
        '200':
          description: Deleted current workspace
          content:
            application/json:
              schema:
                type: object
                required: [success, redirectTo]
                properties:
                  success: { type: boolean }
                  redirectTo: { type: string }
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/members:
    get:
      operationId: listWorkspaceMembers
      responses:
        '200':
          description: Active members and pending invitations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceMembersResponse'
        default:
          $ref: '#/components/responses/Problem'
    patch:
      operationId: updateWorkspaceMemberOrInvitation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MutateWorkspaceMemberRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
    post:
      operationId: resendWorkspaceInvitation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MutateWorkspaceMemberRequest'
      responses:
        '200':
          description: Resent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
    delete:
      operationId: removeWorkspaceMemberOrInvitation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MutateWorkspaceMemberRequest'
      responses:
        '200':
          description: Removed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuccessResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/imports/preview:
    post:
      operationId: previewWorkspaceImport
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkspaceImportPreviewRequest'
      responses:
        '200':
          description: Workspace import preview
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceImportPreviewResponse'
        default:
          $ref: '#/components/responses/Problem'
  /workspaces/invite:
    post:
      operationId: inviteWorkspaceMembers
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InviteWorkspaceMembersRequest'
      responses:
        '200':
          description: Invite results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InviteWorkspaceMembersResponse'
        default:
          $ref: '#/components/responses/Problem'
  /sync/ws:
    get:
      operationId: watchSync
      summary: Open a workspace sync WebSocket and replay operations after a version
      parameters:
        - name: version
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: access_token
          in: query
          description: Browser/CLI WebSocket auth fallback when Authorization headers are unavailable.
          schema: { type: string }
      responses:
        '101':
          description: WebSocket protocol switch
        default:
          $ref: '#/components/responses/Problem'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: lin_api or pat
  parameters:
    Cursor:
      name: cursor
      in: query
      schema: { type: string }
    Limit:
      name: limit
      in: query
      schema: { type: integer, minimum: 1, maximum: 100, default: 50 }
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      schema: { type: string, minLength: 1, maxLength: 255 }
  responses:
    Problem:
      description: RFC 7807 problem response
      content:
        application/problem+json:
          schema:
            $ref: '#/components/schemas/Problem'
  schemas:
    Problem:
      type: object
      required: [type, title, status]
      properties:
        type: { type: string, format: uri-reference }
        title: { type: string }
        status: { type: integer }
        detail: { type: string }
    AccountProfile:
      type: object
      required: [name, email, username, pronouns, title, location, timezone, showLocalTime]
      properties:
        name: { type: string }
        email: { type: string }
        username: { type: string }
        pronouns: { type: string }
        title: { type: string }
        location: { type: string }
        timezone: { type: string }
        showLocalTime: { type: boolean }
        image: { type: [string, 'null'] }
    WorkspaceAccess:
      type: object
      properties:
        currentWorkspaceId: { type: [string, 'null'], format: uuid }
        currentWorkspaceName: { type: [string, 'null'] }
    AccountProfileResponse:
      type: object
      required: [profile, workspaceAccess]
      properties:
        profile: { $ref: '#/components/schemas/AccountProfile' }
        workspaceAccess: { $ref: '#/components/schemas/WorkspaceAccess' }
    UpdateAccountProfileRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1 }
        username: { type: string }
        image: { type: [string, 'null'] }
        pronouns: { type: string }
        title: { type: string }
        location: { type: string }
        timezone: { type: string }
        showLocalTime: { type: boolean }
    AccountNotificationsResponse:
      type: object
      required: [accountNotifications]
      properties:
        accountNotifications:
          type: object
          additionalProperties: true
    UpdateAccountNotificationsRequest:
      type: object
      required: [accountNotifications]
      properties:
        accountNotifications:
          type: object
          additionalProperties: true
    LeaveWorkspaceResponse:
      type: object
      required: [success, redirectTo]
      properties:
        success: { type: boolean }
        redirectTo: { type: string }
    AccountPreferencesResponse:
      type: object
      required: [accountPreferences]
      properties:
        accountPreferences:
          type: object
          additionalProperties: true
    UpdateAccountPreferencesRequest:
      type: object
      required: [accountPreferences]
      properties:
        accountPreferences:
          type: object
          additionalProperties: true
    AnalyticsCompletedTeamCount:
      type: object
      required: [teamId, teamName, completedCount]
      properties:
        teamId: { type: string, format: uuid }
        teamName: { type: string }
        completedCount: { type: integer }
    AnalyticsActiveTeamCount:
      type: object
      required: [teamId, teamName, activeCount]
      properties:
        teamId: { type: string, format: uuid }
        teamName: { type: string }
        activeCount: { type: integer }
    WorkspaceAnalyticsResponse:
      type: object
      required: [workspaceId, completedLast30Days, activeIssues, period]
      properties:
        workspaceId: { type: string, format: uuid }
        completedLast30Days:
          type: array
          items: { $ref: '#/components/schemas/AnalyticsCompletedTeamCount' }
        activeIssues:
          type: array
          items: { $ref: '#/components/schemas/AnalyticsActiveTeamCount' }
        period: { type: string }
    SamlDiscoveryRequest:
      type: object
      required: [email]
      properties:
        email: { type: string }
        isDesktop: { type: boolean }
        type: { type: string }
        callbackURL: { type: string }
    SamlDiscoveryResponse:
      type: object
      required: [url]
      properties:
        url: { type: string, format: uri }
    AccountProviderCapability:
      type: object
      required: [supported, configured, devLinking, unavailableReason]
      properties:
        supported: { type: boolean }
        configured: { type: boolean }
        devLinking: { type: boolean }
        unavailableReason: { type: [string, 'null'] }
    WorkspaceAuthenticationSettings:
      type: object
      required: [google, emailPasskey]
      properties:
        google: { type: boolean }
        emailPasskey: { type: boolean }
    AuthProviderWorkspace:
      type: object
      required: [slug, authentication]
      properties:
        slug: { type: string }
        authentication: { $ref: '#/components/schemas/WorkspaceAuthenticationSettings' }
    AgentGuidanceEntry:
      type: object
      required: [Source, Label, Instructions]
      properties:
        Source: { type: string }
        Label: { type: string }
        Instructions: { type: string }
    AgentGuidance:
      type: object
      required: [entries, effectiveInstructions, autoFixEnabled]
      properties:
        entries:
          type: array
          items: { $ref: '#/components/schemas/AgentGuidanceEntry' }
        effectiveInstructions: { type: string }
        autoFixEnabled: { type: boolean }
        teamKey: { type: [string, 'null'] }
    AgentSuggestion:
      type: object
      required: [id, title, summary, target, contextUrl, status]
      properties:
        id: { type: string }
        title: { type: string }
        summary: { type: string }
        target: { type: string }
        contextUrl: { type: string }
        isExternalContext: { type: boolean }
        status: { type: string, enum: [open, accepted, declined] }
    AgentRun:
      type: object
      required: [id, title, prompt, teamKey, promptConfig, context, status, owner, target, createdAt, updatedAt, output, logs, suggestions]
      properties:
        id: { type: string }
        title: { type: string }
        prompt: { type: string }
        teamKey: { type: string }
        promptConfig:
          type: object
          required: [guidance]
          properties:
            guidance: { $ref: '#/components/schemas/AgentGuidance' }
        context: { type: string }
        status: { type: string, enum: [queued, running, needs_review, completed] }
        owner: { type: string }
        target: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        output: { type: string }
        logs:
          type: array
          items: { type: string }
        suggestions:
          type: array
          items: { $ref: '#/components/schemas/AgentSuggestion' }
    AgentRunListResponse:
      type: object
      required: [runs, canCreateRuns]
      properties:
        runs:
          type: array
          items: { $ref: '#/components/schemas/AgentRun' }
        canCreateRuns: { type: boolean }
    AgentRunResponse:
      type: object
      required: [run]
      properties:
        run: { $ref: '#/components/schemas/AgentRun' }
    CreateAgentRunRequest:
      type: object
      required: [title, prompt]
      properties:
        title: { type: string }
        prompt: { type: string }
        teamKey: { type: string }
        context: { type: string }
    AuthProviderCapabilitiesResponse:
      type: object
      required: [providers, workspace]
      properties:
        providers:
          type: object
          required: [google, github, gitlab, slack, passkey, googleAllowed, emailPasskey]
          properties:
            google: { $ref: '#/components/schemas/AccountProviderCapability' }
            github: { $ref: '#/components/schemas/AccountProviderCapability' }
            gitlab: { $ref: '#/components/schemas/AccountProviderCapability' }
            slack: { $ref: '#/components/schemas/AccountProviderCapability' }
            passkey: { type: boolean }
            googleAllowed: { type: boolean }
            emailPasskey: { type: boolean }
        workspace:
          anyOf:
            - $ref: '#/components/schemas/AuthProviderWorkspace'
            - type: 'null'
    CommentUser:
      type: object
      required: [name]
      properties:
        name: { type: string }
        image: { type: [string, 'null'] }
    ReactionSummary:
      type: object
      required: [emoji, count]
      properties:
        emoji: { type: string }
        count: { type: integer }
        reacted: { type: boolean }
        reactedByMe: { type: boolean }
    ReactionSummaryList:
      type: array
      items: { $ref: '#/components/schemas/ReactionSummary' }
    Comment:
      type: object
      required: [id, body, issue_id, user_id, user, owned_by_me, can_edit, can_delete, reactions, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        body: { type: string }
        issue_id: { type: string, format: uuid }
        user_id: { type: string }
        user: { $ref: '#/components/schemas/CommentUser' }
        owned_by_me: { type: boolean }
        can_edit: { type: boolean }
        can_delete: { type: boolean }
        reactions:
          type: array
          items: { $ref: '#/components/schemas/ReactionSummary' }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CreateCommentRequest:
      type: object
      required: [body]
      properties:
        body: { type: string, minLength: 1 }
    UpdateCommentRequest:
      type: object
      required: [body]
      properties:
        body: { type: string, minLength: 1 }
    ReactionRequest:
      type: object
      required: [emoji]
      properties:
        emoji: { type: string, minLength: 1, maxLength: 50 }
    CustomEmoji:
      type: object
      required: [id, name, imageUrl, createdAt]
      properties:
        id: { type: string }
        name: { type: string }
        imageUrl: { type: string }
        createdAt: { type: string, format: date-time }
    CustomEmojiListResponse:
      type: object
      required: [emojis]
      properties:
        emojis:
          type: array
          items: { $ref: '#/components/schemas/CustomEmoji' }
    CustomEmojiResponse:
      type: object
      required: [emoji]
      properties:
        emoji: { $ref: '#/components/schemas/CustomEmoji' }
    CreateCustomEmojiRequest:
      type: object
      required: [name, imageUrl]
      properties:
        name: { type: string, minLength: 1, maxLength: 32 }
        imageUrl: { type: string }
    DocumentTemplate:
      type: object
      required: [id, name, description, content, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        content: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    DocumentFolder:
      type: object
      required: [id, name, description, color, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
        color: { type: string, enum: [gray, blue, green, yellow, orange, purple, pink] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    DocumentSettings:
      type: object
      required: [templates, folders]
      properties:
        templates:
          type: array
          items: { $ref: '#/components/schemas/DocumentTemplate' }
        folders:
          type: array
          items: { $ref: '#/components/schemas/DocumentFolder' }
    DocumentSettingsResponse:
      type: object
      required: [documents]
      properties:
        documents: { $ref: '#/components/schemas/DocumentSettings' }
    DocumentFolderResponse:
      type: object
      required: [folder]
      properties:
        folder: { $ref: '#/components/schemas/DocumentFolder' }
    DocumentTemplateResponse:
      type: object
      required: [template]
      properties:
        template: { $ref: '#/components/schemas/DocumentTemplate' }
    DocumentFolderRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        color: { type: string, enum: [gray, blue, green, yellow, orange, purple, pink] }
    DocumentTemplateRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        content: { type: string }
    InboundTeamEmailRequest:
      type: object
      properties:
        recipient: { type: string }
        to: { type: string }
        from: { type: string }
        sender: { type: string }
        subject: { type: string }
        text: { type: string }
        html: { type: string }
    InboundTeamEmailResponse:
      type: object
      required: [issue]
      properties:
        issue:
          type: object
          additionalProperties: true
    IntegrationSetupRequirement:
      type: object
      required: [type, message]
      properties:
        type: { type: string }
        message: { type: string }
    IntegrationActions:
      type: object
      required: [canConnect, canManage, canDisconnect]
      properties:
        canConnect: { type: boolean }
        canManage: { type: boolean }
        canDisconnect: { type: boolean }
    Integration:
      type: object
      required: [provider, name, description, id, status, displayName, externalId, connectedAt, setupRequirement, actions]
      properties:
        provider: { type: string, enum: [github, slack, zendesk] }
        name: { type: string }
        description: { type: string }
        id: { type: [string, 'null'], format: uuid }
        status: { type: string }
        displayName: { type: [string, 'null'] }
        externalId: { type: [string, 'null'] }
        connectedAt: { type: [string, 'null'], format: date-time }
        setupRequirement:
          anyOf:
            - $ref: '#/components/schemas/IntegrationSetupRequirement'
            - type: 'null'
        actions: { $ref: '#/components/schemas/IntegrationActions' }
    SlackConnectResponse:
      type: object
      required: [authorizationUrl, state, workspaceSlug]
      properties:
        authorizationUrl: { type: string, format: uri }
        state: { type: string }
        workspaceSlug: { type: string }
    SlackConfigurationRequiredResponse:
      type: object
      required: [error, message]
      properties:
        error: { type: string }
        message: { type: string }
    IntegrationListResponse:
      type: object
      required: [canManageIntegrations, integrations]
      properties:
        canManageIntegrations: { type: boolean }
        integrations:
          type: array
          items: { $ref: '#/components/schemas/Integration' }
    Label:
      type: object
      required: [id, name, color, scope, issueCount, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        color: { type: string }
        description: { type: [string, 'null'] }
        parentLabelId: { type: [string, 'null'], format: uuid }
        teamId: { type: [string, 'null'], format: uuid }
        teamName: { type: [string, 'null'] }
        teamKey: { type: [string, 'null'] }
        scope: { type: string, enum: [workspace, team] }
        archivedAt: { type: [string, 'null'], format: date-time }
        issueCount: { type: integer }
        lastApplied: { type: [string, 'null'], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    LabelListResponse:
      type: object
      required: [labels]
      properties:
        labels:
          type: array
          items: { $ref: '#/components/schemas/Label' }
    LabelResponse:
      type: object
      required: [label]
      properties:
        label: { $ref: '#/components/schemas/Label' }
    CreateLabelRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        color: { type: string }
        description: { type: [string, 'null'] }
        parentLabelId: { type: [string, 'null'], format: uuid }
        teamId: { type: [string, 'null'], format: uuid }
    UpdateLabelRequest:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        color: { type: string }
        description: { type: [string, 'null'] }
        parentLabelId: { type: [string, 'null'], format: uuid }
        teamId: { type: [string, 'null'], format: uuid }
        archived: { type: boolean }
        convertToGroup: { type: boolean }
    BulkLabelRequest:
      type: object
      required: [action, labelIds]
      properties:
        action: { type: string, enum: [archive, unarchive, delete, convertToGroup, rescope, merge] }
        labelIds:
          type: array
          items: { type: string, format: uuid }
        destinationLabelId: { type: [string, 'null'], format: uuid }
        teamId: { type: [string, 'null'], format: uuid }
    BulkLabelResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
        updatedCount: { type: integer }
        destinationLabelId: { type: [string, 'null'], format: uuid }
        mergedCount: { type: integer }
    ProjectLabel:
      type: object
      required: [id, name, color, projectCount, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        color: { type: string }
        description: { type: [string, 'null'] }
        projectCount: { type: integer }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ProjectLabelListResponse:
      type: object
      required: [labels]
      properties:
        labels:
          type: array
          items: { $ref: '#/components/schemas/ProjectLabel' }
    ProjectLabelResponse:
      type: object
      required: [label]
      properties:
        label: { $ref: '#/components/schemas/ProjectLabel' }
    CreateProjectLabelRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        color: { type: string }
        description: { type: [string, 'null'] }
    UpdateProjectLabelRequest:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        color: { type: string }
        description: { type: [string, 'null'] }
    MyIssuesState:
      type: object
      required: [id, name, category, color, position]
      properties:
        id: { type: string }
        name: { type: string }
        category: { type: string }
        color: { type: string }
        position: { type: integer }
    MyIssuesLabel:
      type: object
      required: [id, name, color]
      properties:
        id: { type: string }
        name: { type: string }
        color: { type: string }
    MyIssuesAssignee:
      type: object
      required: [name]
      properties:
        id: { type: string }
        name: { type: string }
        image: { type: [string, 'null'] }
    MyIssuesIssue:
      type: object
      required: [id, number, identifier, title, priority, stateId, labels, labelIds, createdAt, updatedAt, displayAt, teamKey]
      properties:
        id: { type: string, format: uuid }
        number: { type: integer }
        identifier: { type: string }
        title: { type: string }
        priority: { type: string }
        stateId: { type: string }
        assigneeId: { type: [string, 'null'] }
        assignee: { anyOf: [{ $ref: '#/components/schemas/MyIssuesAssignee' }, { type: 'null' }] }
        labels:
          type: array
          items: { $ref: '#/components/schemas/MyIssuesLabel' }
        labelIds:
          type: array
          items: { type: string }
        projectId: { type: [string, 'null'], format: uuid }
        projectName: { type: [string, 'null'] }
        dueDate: { type: [string, 'null'], format: date-time }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        displayAt: { type: string, format: date-time }
        teamKey: { type: string }
    MyIssuesGroup:
      type: object
      required: [state, issues]
      properties:
        state: { $ref: '#/components/schemas/MyIssuesState' }
        issues:
          type: array
          items: { $ref: '#/components/schemas/MyIssuesIssue' }
    MyIssuesFilterOption:
      type: object
      properties:
        value: { type: string }
        id: { type: string }
        label: { type: string }
        name: { type: string }
        color: { type: string }
        image: { type: [string, 'null'] }
    MyIssuesFilterOptions:
      type: object
      required: [statuses, assignees, labels, priorities]
      properties:
        statuses:
          type: array
          items: { $ref: '#/components/schemas/MyIssuesFilterOption' }
        assignees:
          type: array
          items: { $ref: '#/components/schemas/MyIssuesFilterOption' }
        labels:
          type: array
          items: { $ref: '#/components/schemas/MyIssuesFilterOption' }
        priorities:
          type: array
          items: { $ref: '#/components/schemas/MyIssuesFilterOption' }
    MyIssuesResponse:
      type: object
      required: [groups, filterOptions]
      properties:
        groups:
          type: array
          items: { $ref: '#/components/schemas/MyIssuesGroup' }
        totalCount: { type: integer }
        filterOptions: { $ref: '#/components/schemas/MyIssuesFilterOptions' }
    Notification:
      type: object
      required: [id, type, actorName, issueIdentifier, issueTitle, issuePriority, createdAt]
      properties:
        id: { type: string, format: uuid }
        type: { type: string, enum: [assigned, mentioned, status_change, comment, duplicate] }
        actorName: { type: string }
        actorImage: { type: [string, 'null'] }
        issueIdentifier: { type: string }
        issueTitle: { type: string }
        issuePriority: { type: string }
        issueId: { type: [string, 'null'], format: uuid }
        readAt: { type: [string, 'null'], format: date-time }
        snoozedUntilAt: { type: [string, 'null'], format: date-time }
        unsnoozedAt: { type: [string, 'null'], format: date-time }
        createdAt: { type: string, format: date-time }
    NotificationListResponse:
      type: object
      required: [notifications, unreadCount]
      properties:
        notifications:
          type: array
          items: { $ref: '#/components/schemas/Notification' }
        unreadCount: { type: integer }
    BulkReadNotificationsResponse:
      type: object
      required: [success, updatedCount, unreadCount]
      properties:
        success: { type: boolean }
        updatedCount: { type: integer }
        unreadCount: { type: integer }
    NotificationSnoozeInfo:
      type: object
      required: [id, snoozedUntilAt, unsnoozedAt]
      properties:
        id: { type: string, format: uuid }
        snoozedUntilAt: { type: [string, 'null'], format: date-time }
        unsnoozedAt: { type: [string, 'null'], format: date-time }
    NotificationActionResponse:
      type: object
      required: [success, unreadCount]
      properties:
        success: { type: boolean }
        unreadCount: { type: integer }
        notification:
          anyOf:
            - $ref: '#/components/schemas/NotificationSnoozeInfo'
            - type: 'null'
    SnoozeNotificationRequest:
      type: object
      properties:
        snoozedUntilAt: { type: [string, 'null'], format: date-time }
    PersonalAccessToken:
      type: object
      required: [id, name, token_prefix, scopes, created_at]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        token_prefix: { type: string }
        scopes:
          type: array
          items: { type: string }
        created_at: { type: string, format: date-time }
        last_used_at: { type: [string, 'null'], format: date-time }
        revoked_at: { type: [string, 'null'], format: date-time }
    PersonalAccessTokenListResponse:
      type: object
      required: [tokens]
      properties:
        tokens:
          type: array
          items: { $ref: '#/components/schemas/PersonalAccessToken' }
    CreatePersonalAccessTokenRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        scopes:
          type: array
          items: { type: string }
    CreatePersonalAccessTokenResponse:
      type: object
      required: [token, value]
      properties:
        token: { $ref: '#/components/schemas/PersonalAccessToken' }
        value: { type: string }
    ProjectUpdate:
      type: object
      required: [id, name, enabled, cadence, dueDay, dueTime, timezone, scope, projectIds, reportingTarget, shareTarget, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        enabled: { type: boolean }
        cadence: { type: string, enum: [weekly, biweekly, monthly] }
        dueDay: { type: string, enum: [monday, tuesday, wednesday, thursday, friday] }
        dueTime: { type: string }
        timezone: { type: string }
        scope: { type: string, enum: [all_projects, active_projects, selected_projects] }
        projectIds:
          type: array
          items: { type: string }
        reportingTarget: { type: string, enum: [workspace, slack, email] }
        shareTarget: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ProjectUpdateListResponse:
      type: object
      required: [configurations]
      properties:
        configurations:
          type: array
          items: { $ref: '#/components/schemas/ProjectUpdate' }
    ProjectUpdateResponse:
      type: object
      required: [configuration]
      properties:
        configuration: { $ref: '#/components/schemas/ProjectUpdate' }
    ProjectUpdateRequest:
      type: object
      properties:
        name: { type: string }
        enabled: { type: boolean }
        cadence: { type: string, enum: [weekly, biweekly, monthly] }
        dueDay: { type: string, enum: [monday, tuesday, wednesday, thursday, friday] }
        dueTime: { type: string }
        timezone: { type: string }
        scope: { type: string, enum: [all_projects, active_projects, selected_projects] }
        projectIds:
          type: array
          items: { type: string }
        reportingTarget: { type: string, enum: [workspace, slack, email] }
        shareTarget: { type: string }
    ProjectUpdateConfiguration:
      type: object
      required: [id, name, enabled, cadence, dayOfWeek, timeOfDay, timezone, projectScope, statusScope, shareTargets, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        enabled: { type: boolean }
        cadence: { type: string, enum: [weekly, biweekly, monthly] }
        dayOfWeek: { type: integer, minimum: 1, maximum: 7 }
        timeOfDay: { type: string, pattern: '^([01]\d|2[0-3]):[0-5]\d$' }
        timezone: { type: string }
        projectScope: { type: string, enum: [all, active, statuses] }
        statusScope:
          type: array
          items: { type: string, enum: [planned, started, paused, completed, canceled] }
        shareTargets:
          type: array
          items: { type: string, enum: [workspace, slack, email] }
        slackChannel: { type: [string, 'null'] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ProjectUpdateConfigurationListResponse:
      type: object
      required: [configurations, canManage]
      properties:
        configurations:
          type: array
          items: { $ref: '#/components/schemas/ProjectUpdateConfiguration' }
        canManage: { type: boolean }
    ProjectUpdateConfigurationResponse:
      type: object
      required: [configuration]
      properties:
        configuration: { $ref: '#/components/schemas/ProjectUpdateConfiguration' }
    ProjectUpdateConfigurationRequest:
      type: object
      required: [name, cadence, dayOfWeek, timeOfDay, projectScope, shareTargets]
      properties:
        name: { type: string }
        enabled: { type: boolean }
        cadence: { type: string, enum: [weekly, biweekly, monthly] }
        dayOfWeek: { type: integer, minimum: 1, maximum: 7 }
        timeOfDay: { type: string }
        timezone: { type: string }
        projectScope: { type: string, enum: [all, active, statuses] }
        statusScope:
          type: array
          items: { type: string }
        shareTargets:
          type: array
          items: { type: string }
        slackChannel: { type: [string, 'null'] }
    ProjectTemplateSettings:
      type: object
      required: [status, priority, labelIds, milestones]
      properties:
        status: { type: [string, 'null'], enum: [planned, started, paused, completed, canceled, null] }
        priority: { type: [string, 'null'], enum: [none, urgent, high, medium, low, null] }
        labelIds:
          type: array
          items: { type: string }
        milestones:
          type: array
          items: { type: string }
    ProjectTemplate:
      type: object
      required: [id, name, description, settings, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        settings: { $ref: '#/components/schemas/ProjectTemplateSettings' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ProjectTemplateListResponse:
      type: object
      required: [templates]
      properties:
        templates:
          type: array
          items: { $ref: '#/components/schemas/ProjectTemplate' }
    ProjectTemplateResponse:
      type: object
      required: [template]
      properties:
        template: { $ref: '#/components/schemas/ProjectTemplate' }
    ProjectTemplateRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        description: { type: [string, 'null'] }
        settings: { $ref: '#/components/schemas/ProjectTemplateSettings' }
    ProjectStatusConfig:
      type: object
      required: [id, key, name, description, color, icon, position, isDefault, projectCount]
      properties:
        id: { type: string }
        key: { type: string }
        name: { type: string }
        description: { type: string }
        color:
          type: string
          pattern: '^#[0-9a-fA-F]{6}$'
        icon: { type: string, minLength: 1, maxLength: 4 }
        position: { type: integer }
        isDefault: { type: boolean }
        projectCount: { type: integer }
    ProjectStatusListResponse:
      type: object
      required: [statuses, totalProjects, readOnly, customStatusesSupported, canManage]
      properties:
        statuses:
          type: array
          items: { $ref: '#/components/schemas/ProjectStatusConfig' }
        totalProjects: { type: integer }
        readOnly: { type: boolean }
        customStatusesSupported: { type: boolean }
        canManage: { type: boolean }
    UpdateProjectStatusesRequest:
      type: object
      required: [statuses]
      properties:
        statuses:
          type: array
          items: { $ref: '#/components/schemas/ProjectStatusConfig' }
    SidebarFavoriteObjectType:
      type: string
      enum: [project, issue, view]
    SidebarFavorite:
      type: object
      required: [id, objectType, objectId, createdAt, label, href, context]
      properties:
        id: { type: string }
        objectType: { $ref: '#/components/schemas/SidebarFavoriteObjectType' }
        objectId: { type: string }
        createdAt: { type: string, format: date-time }
        label: { type: string }
        href: { type: string }
        context: { type: [string, 'null'] }
    SidebarFavoriteListResponse:
      type: object
      required: [favorites]
      properties:
        favorites:
          type: array
          items: { $ref: '#/components/schemas/SidebarFavorite' }
    SidebarFavoriteRequest:
      type: object
      required: [objectType, objectId]
      properties:
        objectType: { $ref: '#/components/schemas/SidebarFavoriteObjectType' }
        objectId: { type: string }
    ReorderSidebarFavoritesRequest:
      type: object
      required: [orderedIds]
      properties:
        orderedIds:
          type: array
          items: { type: string }
    ProjectStatus:
      type: string
      enum: [planned, started, paused, completed, canceled]
    ProjectPriority:
      type: string
      enum: [none, urgent, high, medium, low]
    ProjectTeam:
      type: object
      required: [id, key, name]
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
    ProjectProgress:
      type: object
      required: [total, completed, percentage]
      properties:
        total: { type: integer }
        completed: { type: integer }
        percentage: { type: integer }
    Project:
      type: object
      required: [id, name, slug, status, priority, workspace_id, teams, progress, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: [string, 'null'] }
        icon: { type: [string, 'null'] }
        slug: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        priority: { $ref: '#/components/schemas/ProjectPriority' }
        lead_id: { type: [string, 'null'] }
        workspace_id: { type: string, format: uuid }
        start_date: { type: [string, 'null'], format: date-time }
        target_date: { type: [string, 'null'], format: date-time }
        completed_at: { type: [string, 'null'], format: date-time }
        canceled_at: { type: [string, 'null'], format: date-time }
        teams:
          type: array
          items: { $ref: '#/components/schemas/ProjectTeam' }
        progress: { $ref: '#/components/schemas/ProjectProgress' }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    ProjectMilestone:
      type: object
      required: [id, name, sortOrder]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        sortOrder: { type: number }
        description: { type: [string, 'null'] }
        issueCount: { type: integer }
        completedCount: { type: integer }
        progress: { type: integer }
    ProjectMilestoneResponse:
      type: object
      required: [milestone]
      properties:
        milestone:
          $ref: '#/components/schemas/ProjectMilestone'
    ProjectMilestoneRequest:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        description: { type: [string, 'null'] }
        sortOrder: { type: number }
    ProjectListResponse:
      type: object
      required: [projects]
      properties:
        projects:
          type: array
          items: { $ref: '#/components/schemas/Project' }
    CreateProjectRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        description: { type: [string, 'null'] }
        icon: { type: [string, 'null'], maxLength: 10 }
        slug: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        priority: { $ref: '#/components/schemas/ProjectPriority' }
        lead_id: { type: [string, 'null'] }
        start_date: { type: [string, 'null'], format: date-time }
        target_date: { type: [string, 'null'], format: date-time }
        team_ids:
          type: array
          items: { type: string, format: uuid }
        team_keys:
          type: array
          items: { type: string }
    UpdateProjectRequest:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        description: { type: [string, 'null'] }
        icon: { type: [string, 'null'], maxLength: 10 }
        slug: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        priority: { $ref: '#/components/schemas/ProjectPriority' }
        lead_id: { type: [string, 'null'] }
        start_date: { type: [string, 'null'], format: date-time }
        target_date: { type: [string, 'null'], format: date-time }
        team_ids:
          type: array
          items: { type: string, format: uuid }
        team_keys:
          type: array
          items: { type: string }
    Team:
      type: object
      required: [id, name, key, isPrivate, issueCount, memberCount, currentUserIsMember, createdAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        key: { type: string }
        icon: { type: [string, 'null'] }
        isPrivate: { type: boolean }
        issueCount: { type: integer }
        memberCount: { type: integer }
        currentUserIsMember: { type: boolean }
        createdAt: { type: string, format: date-time }
        retiredAt: { type: [string, 'null'], format: date-time }
    TeamContextTeam:
      type: object
      required: [id, name, key]
      properties:
        id: { type: string }
        name: { type: string }
        key: { type: string }
        parentTeamId: { type: [string, 'null'] }
        retiredAt: { type: [string, 'null'] }
    TeamContextResponse:
      type: object
      required: [workspaceName, workspaceSlug, workspaceId, teamId, teamName, teamKey, workspaceInitials, teams]
      properties:
        workspaceName: { type: string }
        workspaceSlug: { type: string }
        workspaceId: { type: string }
        teamId: { type: string }
        teamName: { type: string }
        teamKey: { type: string }
        workspaceInitials: { type: string }
        teams:
          type: array
          items:
            $ref: '#/components/schemas/TeamContextTeam'
    TeamDisplayOptionsResponse:
      type: object
      required: [displayOptions]
      properties:
        displayOptions: {}
    TeamIssuesResponse:
      type: object
      required: [team, groups, filterOptions]
      properties:
        team:
          type: object
          required: [id, name, key]
          properties:
            id: { type: string }
            name: { type: string }
            key: { type: string }
        groups:
          type: array
          items:
            type: object
            additionalProperties: true
        filterOptions:
          type: object
          additionalProperties: true
    TeamListResponse:
      type: object
      required: [workspaceId, viewerRole, canManageTeams, teams]
      properties:
        workspaceId: { type: string, format: uuid }
        viewerRole: { $ref: '#/components/schemas/WorkspaceRole' }
        canManageTeams: { type: boolean }
        teams:
          type: array
          items: { $ref: '#/components/schemas/Team' }
    CreateTeamRequest:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        key: { type: string, maxLength: 10 }
        isPrivate: { type: boolean }
        icon: { type: string, maxLength: 16 }
    CreateTeamResponse:
      type: object
      required: [team]
      properties:
        team: { $ref: '#/components/schemas/Team' }
    CreateIssueOptionsTeam:
      type: object
      required: [id, name, key, cyclesEnabled, estimateType]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        key: { type: string }
        cyclesEnabled: { type: boolean }
        estimateType: { type: string }
    CreateIssueOptionStatus:
      type: object
      required: [id, name, category, color, isDefault, behavior]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        category: { type: string }
        color: { type: string }
        isDefault: { type: [boolean, 'null'] }
        behavior: { type: object, additionalProperties: true }
    CreateIssueOptionUser:
      type: object
      required: [id, name, image]
      properties:
        id: { type: string }
        name: { type: string }
        image: { type: [string, 'null'] }
    CreateIssueOptionLabel:
      type: object
      required: [id, name, color]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        color: { type: string }
    CreateIssueOptionProject:
      type: object
      required: [id, name, icon]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        icon: { type: [string, 'null'] }
    CreateIssueOptionCycle:
      type: object
      required: [id, name, number, startDate, endDate]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        number: { type: integer }
        startDate: { type: string, format: date-time }
        endDate: { type: string, format: date-time }
    CreateIssueOptionTemplate:
      type: object
      required: [id, name, description, settings]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        settings: { type: object, additionalProperties: true }
    CreateIssueOptionRelationIssue:
      type: object
      required: [id, identifier, title]
      properties:
        id: { type: string, format: uuid }
        identifier: { type: string }
        title: { type: string }
    NumberOption:
      type: object
      required: [value, label]
      properties:
        value: { type: integer }
        label: { type: string }
    StringOption:
      type: object
      required: [value, label]
      properties:
        value: { type: string }
        label: { type: string }
    CreateIssueOptionsResponse:
      type: object
      required: [team, statuses, priorities, assignees, labels, projects, cycles, estimates, templates, relationIssues, dueDatePresets]
      properties:
        team: { $ref: '#/components/schemas/CreateIssueOptionsTeam' }
        statuses:
          type: array
          items: { $ref: '#/components/schemas/CreateIssueOptionStatus' }
        priorities:
          type: array
          items: { $ref: '#/components/schemas/StringOption' }
        assignees:
          type: array
          items: { $ref: '#/components/schemas/CreateIssueOptionUser' }
        labels:
          type: array
          items: { $ref: '#/components/schemas/CreateIssueOptionLabel' }
        projects:
          type: array
          items: { $ref: '#/components/schemas/CreateIssueOptionProject' }
        cycles:
          type: array
          items: { $ref: '#/components/schemas/CreateIssueOptionCycle' }
        estimates:
          type: array
          items: { $ref: '#/components/schemas/NumberOption' }
        templates:
          type: array
          items: { $ref: '#/components/schemas/CreateIssueOptionTemplate' }
        relationIssues:
          type: array
          items: { $ref: '#/components/schemas/CreateIssueOptionRelationIssue' }
        dueDatePresets:
          type: array
          items: { $ref: '#/components/schemas/StringOption' }
    Cycle:
      type: object
      required: [id, number, team_id, start_date, end_date, auto_rollover, issue_count, completed_issue_count, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        name: { type: [string, 'null'] }
        number: { type: integer }
        team_id: { type: string, format: uuid }
        start_date: { type: string, format: date-time }
        end_date: { type: string, format: date-time }
        auto_rollover: { type: boolean }
        issue_count: { type: integer }
        completed_issue_count: { type: integer }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CycleTeam:
      type: object
      required: [id, name, key, cyclesEnabled, timezone]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        key: { type: string }
        cyclesEnabled: { type: boolean }
        cycleStartDay: { type: [integer, 'null'] }
        cycleDurationWeeks: { type: [integer, 'null'] }
        timezone: { type: string }
    TeamCyclesResponse:
      type: object
      required: [team, cycles]
      properties:
        team: { $ref: '#/components/schemas/CycleTeam' }
        cycles:
          type: array
          items: { $ref: '#/components/schemas/Cycle' }
    CreateCycleRequest:
      type: object
      required: [start_date, end_date]
      properties:
        name: { type: [string, 'null'] }
        start_date: { type: string }
        end_date: { type: string }
        auto_rollover: { type: boolean }
    UpdateCycleRequest:
      type: object
      properties:
        name: { type: [string, 'null'] }
        start_date: { type: string }
        end_date: { type: string }
        auto_rollover: { type: boolean }
    WorkspaceRole:
      type: string
      enum: [owner, admin, member, guest]
    WorkspaceMembership:
      type: object
      required: [workspaceId, role, workspaceName, workspaceSlug]
      properties:
        workspaceId: { type: string, format: uuid }
        role: { $ref: '#/components/schemas/WorkspaceRole' }
        workspaceName: { type: string }
        workspaceSlug: { type: string }
    WorkspaceImportPreviewRequest:
      type: object
      required: [csv, teamId, mapping]
      properties:
        csv: { type: string }
        teamId: { type: string, format: uuid }
        mapping:
          type: object
          additionalProperties: true
    WorkspaceImportPreviewRow:
      type: object
      required: [row, title, description, priority, status, errors]
      properties:
        row: { type: integer }
        title: { type: string }
        description: { type: string }
        priority: { type: string }
        status: { type: string }
        errors:
          type: array
          items: { type: string }
    WorkspaceImportPreviewResponse:
      type: object
      required: [preview]
      properties:
        preview:
          type: array
          items: { $ref: '#/components/schemas/WorkspaceImportPreviewRow' }
    SamlSettings:
      type: object
      required: [enabled, domains, idpSsoUrl, entityId, certificate, metadataUrl, lastTestedAt, status, lastError]
      properties:
        enabled: { type: boolean }
        domains:
          type: array
          items: { type: string }
        idpSsoUrl: { type: string }
        entityId: { type: string }
        certificate: { type: string }
        metadataUrl: { type: string }
        lastTestedAt: { type: [string, 'null'], format: date-time }
        status: { type: string, enum: [not_configured, configured, verified, error] }
        lastError: { type: [string, 'null'] }
    SamlSettingsPatch:
      type: object
      additionalProperties: true
    SamlSettingsResponse:
      type: object
      required: [saml]
      properties:
        saml: { $ref: '#/components/schemas/SamlSettings' }
    PublicScimToken:
      type: object
      required: [id, name, prefix, createdAt, revokedAt, lastUsedAt]
      properties:
        id: { type: string }
        name: { type: string }
        prefix: { type: string }
        createdAt: { type: string, format: date-time }
        revokedAt: { type: [string, 'null'], format: date-time }
        lastUsedAt: { type: [string, 'null'], format: date-time }
    PublicScimSettings:
      type: object
      required: [enabled, baseUrl, tokens, lastSyncAt, status]
      properties:
        enabled: { type: boolean }
        baseUrl: { type: string }
        tokens:
          type: array
          items: { $ref: '#/components/schemas/PublicScimToken' }
        lastSyncAt: { type: [string, 'null'], format: date-time }
        status: { type: string, enum: [disabled, enabled] }
    ScimSettingsResponse:
      type: object
      required: [scim]
      properties:
        scim: { $ref: '#/components/schemas/PublicScimSettings' }
    CreateScimTokenResponse:
      type: object
      required: [token, scim, created]
      properties:
        token: { type: string }
        scim: { $ref: '#/components/schemas/PublicScimSettings' }
        created: { $ref: '#/components/schemas/PublicScimToken' }
    ApplicationPermissionGroup:
      type: object
      required: [label, descriptions]
      properties:
        label: { type: string }
        descriptions:
          type: array
          items: { type: string }
    ApplicationOwner:
      type: object
      required: [name, email, image]
      properties:
        name: { type: string }
        email: { type: string }
        image: { type: [string, 'null'] }
    WorkspaceApplication:
      type: object
      required: [id, appId, clientId, name, imageUrl, scopes, permissionGroups, webhooksEnabled, createdAt, updatedAt, lastUsedAt, owner]
      properties:
        id: { type: string }
        appId: { type: string }
        clientId: { type: string }
        name: { type: string }
        imageUrl: { type: [string, 'null'] }
        scopes:
          type: array
          items: { type: string }
        permissionGroups:
          type: array
          items: { $ref: '#/components/schemas/ApplicationPermissionGroup' }
        webhooksEnabled: { type: boolean }
        createdAt: { type: [string, 'null'], format: date-time }
        updatedAt: { type: [string, 'null'], format: date-time }
        lastUsedAt: { type: [string, 'null'], format: date-time }
        owner: { $ref: '#/components/schemas/ApplicationOwner' }
    WorkspaceApplicationsResponse:
      type: object
      required: [applications, canManageApplications]
      properties:
        applications:
          type: array
          items: { $ref: '#/components/schemas/WorkspaceApplication' }
        canManageApplications: { type: boolean }
    SlaPolicyCondition:
      type: object
      properties:
        priority: { type: string, enum: [urgent, high, medium, low] }
        teamKey: { type: string }
    SlaPolicy:
      type: object
      required: [id, name, description, responseTimeHours, resolutionTimeHours, enabled, conditions, createdAt, updatedAt]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: [string, 'null'] }
        responseTimeHours: { type: number }
        resolutionTimeHours: { type: number }
        enabled: { type: boolean }
        conditions: { $ref: '#/components/schemas/SlaPolicyCondition' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    SlaSettingsWithPermissions:
      type: object
      required: [policies, canManage]
      properties:
        policies:
          type: array
          items: { $ref: '#/components/schemas/SlaPolicy' }
        canManage: { type: boolean }
    WorkspaceSlaResponse:
      type: object
      required: [sla]
      properties:
        sla: { $ref: '#/components/schemas/SlaSettingsWithPermissions' }
    SlaPolicyResponse:
      type: object
      required: [policy]
      properties:
        policy: { $ref: '#/components/schemas/SlaPolicy' }
    SlaPolicyInput:
      type: object
      properties:
        name: { type: string }
        description: { type: [string, 'null'] }
        responseTimeHours: { type: number }
        resolutionTimeHours: { type: number }
        enabled: { type: boolean }
        conditions: { $ref: '#/components/schemas/SlaPolicyCondition' }
    WorkspaceAiSettings:
      type: object
      required: [aiFeaturesEnabled, askLinearEnabled, issueSuggestionsEnabled, summariesEnabled, autoTriageEnabled, workspaceAgentGuidance, agentUsagePermission]
      properties:
        aiFeaturesEnabled: { type: boolean }
        askLinearEnabled: { type: boolean }
        issueSuggestionsEnabled: { type: boolean }
        summariesEnabled: { type: boolean }
        autoTriageEnabled: { type: boolean }
        workspaceAgentGuidance: { type: string }
        agentUsagePermission: { type: string, enum: [admins, members, anyone] }
    WorkspaceAiCapabilities:
      type: object
      required: [canManageAiSettings, canUseAgents]
      properties:
        canManageAiSettings: { type: boolean }
        canUseAgents: { type: boolean }
    WorkspaceAiLimits:
      type: object
      required: [workspaceAgentGuidanceMaxLength]
      properties:
        workspaceAgentGuidanceMaxLength: { type: integer }
    WorkspaceAiSettingsResponse:
      type: object
      required: [aiSettings, capabilities, limits]
      properties:
        aiSettings: { $ref: '#/components/schemas/WorkspaceAiSettings' }
        capabilities: { $ref: '#/components/schemas/WorkspaceAiCapabilities' }
        limits: { $ref: '#/components/schemas/WorkspaceAiLimits' }
    UpdateWorkspaceAiSettingsRequest:
      type: object
      required: [aiSettings]
      properties:
        aiSettings:
          type: object
          additionalProperties: true
    WorkspaceInitiativeSettings:
      type: object
      required: [enabled, projectRollups, visibility, roadmapMode]
      properties:
        enabled: { type: boolean }
        projectRollups: { type: boolean }
        visibility: { type: string, enum: [workspace, teams] }
        roadmapMode: { type: string, enum: [all, selected] }
    WorkspaceInitiativeSettingsResponse:
      type: object
      required: [initiativesSettings, viewerRole, canManage]
      properties:
        initiativesSettings: { $ref: '#/components/schemas/WorkspaceInitiativeSettings' }
        viewerRole: { type: string }
        canManage: { type: boolean }
    UpdateWorkspaceInitiativeSettingsRequest:
      type: object
      properties:
        enabled: { type: boolean }
        projectRollups: { type: boolean }
        visibility: { type: string, enum: [workspace, teams] }
        roadmapMode: { type: string, enum: [all, selected] }
    CollaborationAsksSettings:
      type: object
      required: [enabled, intakeEmail, defaultPriority, autoAssign]
      properties:
        enabled: { type: boolean }
        intakeEmail: { type: string }
        defaultPriority: { type: string, enum: [low, medium, high, urgent] }
        autoAssign: { type: boolean }
    CollaborationPulseSettings:
      type: object
      required: [enabled, digestFrequency, burnoutAlerts, velocityTarget]
      properties:
        enabled: { type: boolean }
        digestFrequency: { type: string, enum: [daily, weekly, off] }
        burnoutAlerts: { type: boolean }
        velocityTarget: { type: integer }
    CollaborationCustomerRequestSettings:
      type: object
      required: [enabled, intakeEmail, defaultPriority, autoLinkIssues, requireCompany, confirmationMessage]
      properties:
        enabled: { type: boolean }
        intakeEmail: { type: string }
        defaultPriority: { type: string, enum: [low, medium, high, urgent] }
        autoLinkIssues: { type: boolean }
        requireCompany: { type: boolean }
        confirmationMessage: { type: string }
    WorkspaceCollaborationSettings:
      type: object
      required: [asks, pulse, customerRequests]
      properties:
        asks: { $ref: '#/components/schemas/CollaborationAsksSettings' }
        pulse: { $ref: '#/components/schemas/CollaborationPulseSettings' }
        customerRequests: { $ref: '#/components/schemas/CollaborationCustomerRequestSettings' }
    WorkspaceCollaborationPermissions:
      type: object
      required: [canManage, role]
      properties:
        canManage: { type: boolean }
        role: { type: string }
    WorkspaceCollaborationResponse:
      type: object
      required: [collaboration, permissions]
      properties:
        collaboration: { $ref: '#/components/schemas/WorkspaceCollaborationSettings' }
        permissions: { $ref: '#/components/schemas/WorkspaceCollaborationPermissions' }
    UpdateWorkspaceCollaborationRequest:
      type: object
      additionalProperties: true
    WorkspaceDocumentTemplate:
      type: object
      required: [id, name, description]
      properties:
        id: { type: string }
        name: { type: string }
        description: { type: string }
    WorkspaceDocumentsSettings:
      type: object
      required: [defaultVisibility, autoLinkProjectDocuments, templates]
      properties:
        defaultVisibility: { type: string, enum: [workspace, private] }
        autoLinkProjectDocuments: { type: boolean }
        templates:
          type: array
          items: { $ref: '#/components/schemas/WorkspaceDocumentTemplate' }
    WorkspaceDocumentsResponse:
      type: object
      required: [documents]
      properties:
        documents: { $ref: '#/components/schemas/WorkspaceDocumentsSettings' }
        template: { $ref: '#/components/schemas/WorkspaceDocumentTemplate' }
    UpdateWorkspaceDocumentsRequest:
      type: object
      properties:
        defaultVisibility: { type: string, enum: [workspace, private] }
        autoLinkProjectDocuments: { type: boolean }
    CreateWorkspaceDocumentTemplateRequest:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
    BillingPlan:
      type: object
      required: [id, name, price, description, features]
      properties:
        id: { type: string, enum: [free, basic, business, enterprise] }
        name: { type: string }
        price: { type: string }
        description: { type: string }
        features:
          type: array
          items: { type: string }
    BillingWorkspace:
      type: object
      required: [id, name, urlSlug, role]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        urlSlug: { type: string }
        role: { type: string }
    BillingUsage:
      type: object
      required: [seatsUsed, issuesUsed, issueLimit]
      properties:
        seatsUsed: { type: integer }
        issuesUsed: { type: integer }
        issueLimit: { type: integer }
    WorkspaceBillingResponse:
      type: object
      required: [workspace, currentPlan, canManage, usage, plans, paymentMethods, invoices]
      properties:
        workspace: { $ref: '#/components/schemas/BillingWorkspace' }
        currentPlan: { type: string, enum: [free, basic, business, enterprise] }
        canManage: { type: boolean }
        usage: { $ref: '#/components/schemas/BillingUsage' }
        plans:
          type: array
          items: { $ref: '#/components/schemas/BillingPlan' }
        paymentMethods:
          type: array
          items: {}
        invoices:
          type: array
          items: {}
    UpdateWorkspaceBillingRequest:
      type: object
      required: [plan]
      properties:
        plan: { type: string, enum: [free, basic, business, enterprise] }
    ViewTeam:
      type: object
      required: [id, key, name]
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
    ViewOwner:
      type: object
      required: [name]
      properties:
        name: { type: string }
        image: { type: [string, 'null'] }
    ViewFilterCondition:
      type: object
      required: [type, operator, values]
      properties:
        type: { type: string }
        operator: { type: string, enum: [is, isNot] }
        values:
          type: array
          items: { type: string }
    ViewIssueDisplayOptions:
      type: object
      required: [groupBy, subGroupBy, orderBy, displayProperties, showSubIssues, showTriageIssues, showEmptyColumns]
      properties:
        groupBy: { type: string, enum: [status, priority, assignee, label, project, none] }
        subGroupBy: { type: string, enum: [status, priority, assignee, label, project, none] }
        orderBy: { type: string, enum: [priority, created, updated, manual] }
        displayProperties:
          type: object
          additionalProperties: { type: boolean }
        showSubIssues: { type: boolean }
        showTriageIssues: { type: boolean }
        showEmptyColumns: { type: boolean }
    ViewProjectDisplayOptions:
      type: object
      required: [groupBy, visibleProperties]
      properties:
        groupBy: { type: string, enum: [status, lead, team, none] }
        visibleProperties:
          type: object
          additionalProperties: { type: boolean }
    ViewFilterState:
      type: object
      required: [entityType, scope, issueFilters, issueDisplayOptions, projectStatusFilter, projectSortBy, projectDisplayOptions]
      properties:
        entityType: { type: string, enum: [issues, projects] }
        scope: { type: string, enum: [team, workspace] }
        issueFilters:
          type: array
          items: { $ref: '#/components/schemas/ViewFilterCondition' }
        issueDisplayOptions: { $ref: '#/components/schemas/ViewIssueDisplayOptions' }
        projectStatusFilter: { type: string }
        projectSortBy: { type: string, enum: [created-desc, created-asc, name-asc, progress-desc, target-date-asc] }
        projectDisplayOptions: { $ref: '#/components/schemas/ViewProjectDisplayOptions' }
    View:
      type: object
      required: [id, name, layout, isPersonal, filterState, entityType, scope, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        layout: { type: string, enum: [list, board, timeline] }
        isPersonal: { type: boolean }
        filterState: { $ref: '#/components/schemas/ViewFilterState' }
        entityType: { type: string, enum: [issues, projects] }
        scope: { type: string, enum: [team, workspace] }
        teamId: { type: [string, 'null'], format: uuid }
        teamKey: { type: [string, 'null'] }
        teamName: { type: [string, 'null'] }
        owner: { anyOf: [{ $ref: '#/components/schemas/ViewOwner' }, { type: 'null' }] }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ViewListResponse:
      type: object
      required: [views, teams]
      properties:
        views:
          type: array
          items: { $ref: '#/components/schemas/View' }
        teams:
          type: array
          items: { $ref: '#/components/schemas/ViewTeam' }
    ViewResponse:
      type: object
      required: [view]
      properties:
        view: { $ref: '#/components/schemas/View' }
    ViewRequest:
      type: object
      properties:
        name: { type: string }
        layout: { type: string, enum: [list, board, timeline] }
        isPersonal: { type: boolean }
        teamId: { type: [string, 'null'], format: uuid }
        filterState: { $ref: '#/components/schemas/ViewFilterState' }
    Workspace:
      type: object
      required: [id, name, urlSlug, region, fiscalMonth, welcomeMessage, plan]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        urlSlug: { type: string }
        logo: { type: [string, 'null'] }
        region: { type: string }
        fiscalMonth: { type: string }
        welcomeMessage: { type: string }
        plan: { type: string }
    WorkspaceSummary:
      type: object
      required: [id, name, urlSlug, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        urlSlug: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    TeamSummary:
      type: object
      required: [id, name, key, workspaceId]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        key: { type: string }
        workspaceId: { type: string, format: uuid }
    CreateWorkspaceRequest:
      type: object
      required: [name, urlSlug]
      properties:
        name: { type: string, minLength: 1, maxLength: 255 }
        urlSlug: { type: string, minLength: 2, maxLength: 63 }
    CreateWorkspaceResponse:
      type: object
      required: [workspace, team]
      properties:
        workspace: { $ref: '#/components/schemas/WorkspaceSummary' }
        team: { $ref: '#/components/schemas/TeamSummary' }
    CurrentWorkspaceResponse:
      type: object
      required: [workspace]
      properties:
        workspace: { $ref: '#/components/schemas/Workspace' }
    UpdateWorkspaceRequest:
      type: object
      properties:
        name: { type: string }
        urlSlug: { type: string }
        logo: { type: [string, 'null'] }
        fiscalMonth: { type: string }
        welcomeMessage: { type: string }
    WorkspaceMember:
      type: object
      required: [id, kind, name, email, role, status, teams, joinedAt]
      properties:
        id: { type: string }
        kind: { type: string, enum: [member, invitation] }
        userId: { type: [string, 'null'] }
        name: { type: string }
        email: { type: string }
        image: { type: [string, 'null'] }
        role: { $ref: '#/components/schemas/WorkspaceRole' }
        status: { type: string, enum: [active, pending] }
        teams:
          type: array
          items: { type: string }
        joinedAt: { type: string, format: date-time }
        lastSeenAt: { type: [string, 'null'], format: date-time }
        pronouns: { type: string }
        title: { type: string }
        location: { type: string }
        timezone: { type: string }
        showLocalTime: { type: boolean }
    WorkspaceMembersResponse:
      type: object
      required: [workspaceId, currentUserId, viewerRole, canInviteMembers, members]
      properties:
        workspaceId: { type: string, format: uuid }
        currentUserId: { type: string }
        viewerRole: { $ref: '#/components/schemas/WorkspaceRole' }
        canInviteMembers: { type: boolean }
        members:
          type: array
          items: { $ref: '#/components/schemas/WorkspaceMember' }
    MutateWorkspaceMemberRequest:
      type: object
      required: [kind, id]
      properties:
        kind: { type: string, enum: [member, invitation] }
        id: { type: string }
        role: { $ref: '#/components/schemas/WorkspaceRole' }
        action: { type: string, enum: [resend] }
    InviteWorkspaceMembersRequest:
      type: object
      required: [invites]
      properties:
        workspaceId: { type: string, format: uuid }
        invites:
          type: array
          items:
            type: object
            required: [email, role]
            properties:
              email: { type: string, format: email }
              role: { type: string, enum: [admin, member, guest] }
    InviteResult:
      type: object
      required: [email, status]
      properties:
        email: { type: string }
        status: { type: string, enum: [sent, failed] }
        error: { type: string }
    InviteWorkspaceMembersResponse:
      type: object
      required: [results]
      properties:
        results:
          type: array
          items: { $ref: '#/components/schemas/InviteResult' }
    SuccessResponse:
      type: object
      required: [success]
      properties:
        success: { type: boolean }
    IssuePriority:
      type: string
      enum: [none, urgent, high, medium, low]
    Issue:
      type: object
      required: [id, number, identifier, title, team_id, state_id, creator_id, priority, created_at, updated_at]
      properties:
        id: { type: string, format: uuid }
        number: { type: integer }
        identifier: { type: string }
        title: { type: string }
        description: { type: [string, 'null'] }
        team_id: { type: string, format: uuid }
        state_id: { type: string, format: uuid }
        assignee_id: { type: [string, 'null'] }
        creator_id: { type: string }
        priority: { $ref: '#/components/schemas/IssuePriority' }
        estimate: { type: [number, 'null'] }
        parent_issue_id: { type: [string, 'null'], format: uuid }
        project_id: { type: [string, 'null'], format: uuid }
        project_milestone_id: { type: [string, 'null'], format: uuid }
        cycle_id: { type: [string, 'null'], format: uuid }
        due_date: { type: [string, 'null'], format: date-time }
        sort_order: { type: number }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        archived_at: { type: [string, 'null'], format: date-time }
        canceled_at: { type: [string, 'null'], format: date-time }
        completed_at: { type: [string, 'null'], format: date-time }
    IssueListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: array
          items: { $ref: '#/components/schemas/Issue' }
        next_cursor: { type: [string, 'null'] }
    IssueTemplateSettings:
      type: object
      properties:
        title: { type: string }
        body: { type: string }
        defaultPriority: { type: string, enum: [urgent, high, medium, low, none] }
        defaultStatusId: { type: string }
        defaultStatusName: { type: string }
        defaultTeamId: { type: string }
        defaultTeamKey: { type: string }
        defaultScope: { type: string }
        defaultProjectId: { type: [string, 'null'] }
        archivedAt: { type: string }
    IssueTemplate:
      type: object
      required: [id, name, description, type, teamId, settings, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        type: { type: string }
        teamId: { type: [string, 'null'], format: uuid }
        settings: { $ref: '#/components/schemas/IssueTemplateSettings' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    IssueTemplateListResponse:
      type: object
      required: [templates]
      properties:
        templates:
          type: array
          items: { $ref: '#/components/schemas/IssueTemplate' }
    IssueTemplateResponse:
      type: object
      required: [template]
      properties:
        template: { $ref: '#/components/schemas/IssueTemplate' }
    CreateIssueTemplateRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        settings: { $ref: '#/components/schemas/IssueTemplateSettings' }
        duplicateFromId: { type: string, format: uuid }
    UpdateIssueTemplateRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        settings: { $ref: '#/components/schemas/IssueTemplateSettings' }
        archived: { type: boolean }
    IssueSearchResult:
      type: object
      required: [id, identifier, title, priority, stateName, stateCategory, stateColor, assigneeName, assigneeImage, createdAt]
      properties:
        id: { type: string, format: uuid }
        identifier: { type: string }
        title: { type: string }
        priority: { $ref: '#/components/schemas/IssuePriority' }
        stateName: { type: string }
        stateCategory: { type: string }
        stateColor: { type: string }
        assigneeName: { type: [string, 'null'] }
        assigneeImage: { type: [string, 'null'] }
        createdAt: { type: string, format: date-time }
    IssueSubscriptionSummary:
      type: object
      required: [subscribed, watcherCount]
      properties:
        subscribed: { type: boolean }
        watcherCount: { type: integer }
    BulkIssueUpdates:
      type: object
      properties:
        stateId: { type: [string, 'null'], format: uuid }
        assigneeId: { type: [string, 'null'] }
        priority: { $ref: '#/components/schemas/IssuePriority' }
        labelIds:
          type: array
          items: { type: string, format: uuid }
        projectId: { type: [string, 'null'], format: uuid }
        cycleId: { type: [string, 'null'], format: uuid }
        dueDate: { type: [string, 'null'], format: date }
        archive: { type: boolean }
        delete: { type: boolean }
    BulkUpdateIssuesRequest:
      type: object
      required: [issueIds, updates]
      properties:
        issueIds:
          type: array
          maxItems: 200
          items: { type: string, format: uuid }
        updates: { $ref: '#/components/schemas/BulkIssueUpdates' }
    BulkUpdateIssuesResponse:
      type: object
      required: [updatedCount]
      properties:
        updatedCount: { type: integer }
    CreateIssueRequest:
      type: object
      required: [title, team_id]
      properties:
        title: { type: string, minLength: 1, maxLength: 500 }
        description: { type: [string, 'null'] }
        team_id: { type: string, format: uuid }
        state_id: { type: [string, 'null'], format: uuid }
        priority: { $ref: '#/components/schemas/IssuePriority' }
        assignee_id: { type: [string, 'null'] }
        project_id: { type: [string, 'null'], format: uuid }
        project_milestone_id: { type: [string, 'null'], format: uuid }
        cycle_id: { type: [string, 'null'], format: uuid }
        parent_issue_id: { type: [string, 'null'], format: uuid }
        estimate: { type: [number, 'null'] }
        due_date: { type: [string, 'null'], format: date }
    UpdateIssueRequest:
      type: object
      properties:
        title: { type: string, minLength: 1, maxLength: 500 }
        description: { type: [string, 'null'] }
        state_id: { type: string, format: uuid }
        priority: { $ref: '#/components/schemas/IssuePriority' }
        assignee_id: { type: [string, 'null'] }
        project_id: { type: [string, 'null'], format: uuid }
        project_milestone_id: { type: [string, 'null'], format: uuid }
        cycle_id: { type: [string, 'null'], format: uuid }
        parent_issue_id: { type: [string, 'null'], format: uuid }
        estimate: { type: [number, 'null'] }
        due_date: { type: [string, 'null'], format: date }
        sort_order: { type: number }
        archive: { type: boolean }
